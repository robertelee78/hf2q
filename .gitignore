# Top-level model files — do not commit to git.
# NOTE: leading slash so this only matches the top-level `models/` directory
# (which holds GGUFs), not `src/inference/models/` (the module tree).
/models/

# Build artifacts
/target

# BMad project files
_bmad/
_bmad-output/

# Claude Code local config
.claude/
.claude-flow/
.mcp.json
.swarm/
CLAUDE.md

# Git worktrees — long-lived parallel branches (moved from /opt/hf2q-adr013
# sibling-path convention to nested on 2026-04-24 per Robert's directive).
# The `/cfa` skill uses `.cfa-worktrees/` for short-lived competitive runs;
# this `.worktrees/` dir is for long-lived branch worktrees (adr-013, etc.).
.worktrees/
.cfa-worktrees/
.cfa-archive/

# Agentic-QE local SQLite memory (accidentally committed in 9602b06; reverted)
.agentic-qe/

# Runtime logs (per-session, never committed).
logs/
# Vendored crate examples are permanent test assets (added 2026-04-11
# for the 1bNEW.29 NSG sweep microbench harness — see docs/spike-1bNEW29-*.md).
!vendor/candle-metal-kernels/examples/
# 2026-05-16: `examples/` at the repo root is now part of the public
# release (5 self-contained scenarios + README index).  Was gitignored
# during the iter-216 ngram diagnostic when this dir held one-off
# bench scripts; that material moved to scripts/ long ago.

# Runtime output from `hf2q generate --benchmark` (ADR-005 1bNEW.0 metrics
# instrumentation). The committed baseline lives at
# tests/fixtures/metrics_baseline.txt — this repo-root file is the live
# per-run report and must not be committed.
/metrics.txt

# Data artifacts — local databases and lock files
*.rvf
*.rvf.lock
ruvector.db

# Local profiling data and scratch docs
docs/phase0-candle-perkernel-raw.json
scripts/dump_layer_states

# dump_layer_states binary (built separately via g++)
scripts/dump_layer_states
.cargo/
iter*_audit.json

# ADR-005 Phase 2c iter-101 vision E2E harness:
# fixture PNGs are auto-regenerated on every test run via
# materialize_fixture_images() in tests/vision_e2e_vs_mlx_vlm.rs;
# the matrix report is per-run output.
tests/fixtures/vision/*.png
tests/fixtures/vision/last_e2e_report.json

# ADR-033 P6: temporary byte-cmp output (large quantized GGUFs)
/tmp/
models
