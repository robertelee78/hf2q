---
description: Search the live web with the local, provenance-bearing research stack
---

Research this live-web query: $ARGUMENTS

Call `web_search` exactly once with that query. Use and cite the returned
sources. Do not substitute Bash, `webfetch`, guessed URLs, or prior knowledge.
If the tool reports `WEB_SEARCH_FAILED`, report that failure and its route
details honestly instead of claiming that the web contains no results.
