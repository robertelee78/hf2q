name: Release

on:
  workflow_dispatch:
    inputs:
      commit_sha:
        description: Exact main-branch commit to publish
        required: true
        type: string
      version:
        description: Exact crate version to publish
        required: true
        type: string
      cache_gate_run_id:
        description: Successful exact-SHA Cache lifecycle workflow run ID
        required: true
        type: string

permissions:
  actions: read
  contents: write

concurrency:
  # The standalone stable locator is global, so stable releases must never
  # race merely because their version strings differ.
  group: release-stable
  cancel-in-progress: false

jobs:
  publish:
    runs-on: macos-latest
    timeout-minutes: 90
    env:
      EXPECTED_SHA: ${{ inputs.commit_sha }}
      EXPECTED_VERSION: ${{ inputs.version }}
      EXPECTED_CACHE_GATE_RUN_ID: ${{ inputs.cache_gate_run_id }}
      EXPECTED_DEEPSEEK_MODEL_SHA256: ${{ vars.DEEPSEEK4_MODEL_SHA256 }}
      EXPECTED_GEMMA_MODEL_SHA256: ${{ vars.GEMMA4_MODEL_SHA256 }}
      EXPECTED_QWEN_MODEL_SHA256: ${{ vars.QWEN36_MODEL_SHA256 }}
      EXPECTED_QWEN38_MODEL_SHA256: ${{ vars.QWEN38_MODEL_SHA256 }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ inputs.commit_sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Verify immutable release identity
        shell: bash
        run: |
          set -euo pipefail
          actual_sha=$(git rev-parse HEAD)
          test "$actual_sha" = "$EXPECTED_SHA"
          git fetch origin main
          test "$actual_sha" = "$(git rev-parse origin/main)"
          actual_version=$(sed -n 's/^version = "\([^"]*\)"/\1/p' Cargo.toml | head -1)
          test "$actual_version" = "$EXPECTED_VERSION"
          git diff --exit-code

      - name: Require successful exact-SHA CI
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          successes=$(gh run list \
            --commit "$EXPECTED_SHA" \
            --workflow CI \
            --json conclusion,event \
            --jq '[.[] | select(.event == "push" and .conclusion == "success")] | length')
          test "$successes" -ge 1

      - name: Require exact-artifact cross-family cache lifecycle proof
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          run_json=$(gh run view "$EXPECTED_CACHE_GATE_RUN_ID" \
            --json conclusion,event,headSha,workflowName,url)
          test "$(jq -r .workflowName <<<"$run_json")" = "Cache lifecycle"
          test "$(jq -r .event <<<"$run_json")" = "workflow_dispatch"
          test "$(jq -r .headSha <<<"$run_json")" = "$EXPECTED_SHA"
          test "$(jq -r .conclusion <<<"$run_json")" = "success"

          gate_dir="$RUNNER_TEMP/cache-lifecycle-proof"
          gh run download "$EXPECTED_CACHE_GATE_RUN_ID" \
            --name "cache-lifecycle-$EXPECTED_SHA" \
            --dir "$gate_dir"
          manifest="$gate_dir/manifest.json"
          test -s "$manifest"
          # Recompute every powered sub-gate's baseline/final multiset delta
          # from the downloaded raw snapshots. Summary counts alone are not
          # release authority.
          source scripts/qwen36_watchdog_validate.sh
          power_snapshot_manifest="$gate_dir/power-event-snapshots.sha256"
          expected_power_snapshot_sha=$(
            jq -er '.power_event_snapshots_sha256' "$manifest"
          )
          actual_power_snapshot_sha=$(
            shasum -a 256 "$power_snapshot_manifest" | awk '{print $1}'
          )
          test "$actual_power_snapshot_sha" = "$expected_power_snapshot_sha"
          power_snapshot_prefixes=()
          while IFS= read -r prefix; do
            power_snapshot_prefixes+=("$prefix")
          done < <(qwen36_release_power_snapshot_prefixes)
          qwen36_verify_power_snapshot_manifest \
            "$gate_dir" "$power_snapshot_manifest" \
            "${power_snapshot_prefixes[@]}"
          check_receipt() {
            local path=$1
            local selector=$2
            local expected actual
            expected=$(jq -er "$selector" "$manifest")
            actual=$(shasum -a 256 "$gate_dir/$path" | awk '{print $1}')
            test "$actual" = "$expected"
          }
          dependency_provenance_dir="$gate_dir/dependency-provenance"
          dependency_receipt="$dependency_provenance_dir/receipt.json"
          check_receipt dependency-provenance/receipt.json \
            '.receipt_sha256.provenance.dependency'
          bash scripts/verify_release_dependency_provenance.sh verify \
            "$dependency_provenance_dir" Cargo.lock
          jq -e --slurpfile dependency "$dependency_receipt" '
            .dependency_provenance == $dependency[0]
            and .dependency_provenance.package.crate_sha256 == .crate_sha256
            and .dependency_provenance.dependency.name == "mlx-native"
            and .dependency_provenance.dependency.version == "0.10.16"
            and .dependency_provenance.dependency.requirement == "=0.10.16"
            and .dependency_provenance.dependency.source
              == "registry+https://github.com/rust-lang/crates.io-index"
            and .dependency_provenance.dependency.checksum
              == "9236b563fc45c861ec772cb0b29aafc1144bc49b92b229790c7e1b57844fc83b"
          ' "$manifest" >/dev/null
          echo "DEPENDENCY_PROVENANCE_DIR=$dependency_provenance_dir" \
            >> "$GITHUB_ENV"
          check_receipt deepseek/lifecycle/summary.json '.receipt_sha256.deepseek.lifecycle'
          check_receipt deepseek/interactive/summary.json '.receipt_sha256.deepseek.interactive'
          check_receipt deepseek/cached-suffix/summary.json '.receipt_sha256.deepseek.cached_suffix'
          check_receipt deepseek/cooperative-prefill/summary.json '.receipt_sha256.deepseek.cooperative_prefill'
          check_receipt deepseek/decode-cohort/summary.json '.receipt_sha256.deepseek.decode_cohort'
          check_receipt deepseek/full-context-1/envelope.json '.receipt_sha256.deepseek.wave1'
          check_receipt deepseek/full-context-2/envelope.json '.receipt_sha256.deepseek.wave2'
          check_receipt deepseek/full-context-1/thermal/summary.json '.receipt_sha256.deepseek.wave1_thermal'
          check_receipt deepseek/full-context-2/thermal/summary.json '.receipt_sha256.deepseek.wave2_thermal'
          check_receipt fixtures/deepseek4-agentic/prompt-provenance.json '.receipt_sha256.deepseek.prompt_provenance'
          prompt_contract="$gate_dir/fixtures/deepseek4-agentic/deepseek4-agentic-prompt-contract-v2.json"
          prompt_context="$gate_dir/fixtures/deepseek4-agentic/deepseek4-agentic-repo-context.txt"
          prompt_tool_result="$gate_dir/fixtures/deepseek4-agentic/deepseek4-agentic-tool-result-863ea423.toml"
          prompt_provenance="$gate_dir/fixtures/deepseek4-agentic/prompt-provenance.json"
          cmp -s scripts/fixtures/deepseek4-agentic-prompt-contract-v2.json "$prompt_contract"
          cmp -s scripts/fixtures/deepseek4-agentic-repo-context.txt "$prompt_context"
          cmp -s scripts/fixtures/deepseek4-agentic-tool-result-863ea423.toml "$prompt_tool_result"
          prompt_contract_sha=$(shasum -a 256 "$prompt_contract" | awk '{print $1}')
          prompt_provenance_sha=$(shasum -a 256 "$prompt_provenance" | awk '{print $1}')
          test "$prompt_contract_sha" = "$(jq -er '.fixtures.deepseek_agentic.contract_sha256' "$manifest")"
          test "$prompt_provenance_sha" = "$(jq -er '.fixtures.deepseek_agentic.prompt_provenance_sha256' "$manifest")"
          test "$(shasum -a 256 "$prompt_context" | awk '{print $1}')" = "$(jq -er '.fixtures.deepseek_agentic.context_sha256' "$manifest")"
          test "$(shasum -a 256 "$prompt_tool_result" | awk '{print $1}')" = "$(jq -er '.fixtures.deepseek_agentic.tool_result_sha256' "$manifest")"
          jq -e -f scripts/deepseek4_agentic_prompt_contract.jq "$prompt_contract" >/dev/null
          jq -e --slurpfile contract "$prompt_contract" \
            --arg prompt_contract_sha256 "$prompt_contract_sha" \
            --arg model_sha256 "$EXPECTED_DEEPSEEK_MODEL_SHA256" \
            -f scripts/deepseek4_agentic_prompt_provenance.jq \
            "$prompt_provenance" >/dev/null
          for agent in 1 2 3 4; do
            request="$gate_dir/fixtures/deepseek4-agentic/request-agent-$agent.json"
            test "$(stat -f %z "$request")" = \
              "$(jq -er --argjson agent "$agent" '.agents[] | select(.agent == $agent) | .request_bytes' "$prompt_contract")"
            test "$(shasum -a 256 "$request" | awk '{print $1}')" = \
              "$(jq -er --argjson agent "$agent" '.agents[] | select(.agent == $agent) | .request_sha256' "$prompt_contract")"
          done
          cooperative_dir="$gate_dir/deepseek/cooperative-prefill"
          bash scripts/verify_deepseek4_cooperative_prefill_receipt.sh \
            "$cooperative_dir/summary.json" "$cooperative_dir/raw.json" \
            "$cooperative_dir/test.log" "$cooperative_dir/thermal.log" \
            "$cooperative_dir/settle.log" "$EXPECTED_SHA" \
            "$EXPECTED_DEEPSEEK_MODEL_SHA256" \
            "$cooperative_dir/measurement-contention.log" \
            "$cooperative_dir/settle-contention.log"
          decode_cohort_dir="$gate_dir/deepseek/decode-cohort"
          bash scripts/verify_deepseek4_decode_cohort_receipt.sh \
            "$decode_cohort_dir/summary.json" "$decode_cohort_dir/raw.json" \
            "$decode_cohort_dir/test.log" "$decode_cohort_dir/thermal.log" \
            "$decode_cohort_dir/settle.log" "$EXPECTED_SHA" \
            "$EXPECTED_DEEPSEEK_MODEL_SHA256" \
            "$decode_cohort_dir/measurement-contention.log" \
            "$decode_cohort_dir/settle-contention.log"
          for wave in 1 2; do
            wave_dir="$gate_dir/deepseek/full-context-$wave"
            summary_sha=$(shasum -a 256 "$wave_dir/summary.json" | awk '{print $1}')
            test "$summary_sha" = "$(jq -er .summary_sha256 "$wave_dir/envelope.json")"
            jq -e --slurpfile contract "$prompt_contract" \
              --arg prompt_contract_sha256 "$prompt_contract_sha" \
              --arg prompt_provenance_sha256 "$prompt_provenance_sha" \
              -f scripts/deepseek4_full_context_receipt.jq \
              "$wave_dir/summary.json" >/dev/null
            thermal_summary="$wave_dir/thermal/summary.json"
            thermal_log="$wave_dir/thermal/measurement.log"
            thermal_settle_log="$wave_dir/thermal/settle.log"
            contention_log="$wave_dir/thermal/measurement-contention.log"
            contention_settle_log="$wave_dir/thermal/settle-contention.log"
            bash scripts/verify_macos_thermal_receipt.sh "$wave" \
              "$wave_dir/envelope.json" "$thermal_summary" \
              "$thermal_log" "$thermal_settle_log" "$wave_dir/agents" \
              "$contention_log" "$contention_settle_log"
            server_log="$gate_dir/deepseek/process-wave-$wave/server.log"
            test -s "$server_log"
            test "$(shasum -a 256 "$server_log" | awk '{print $1}')" = \
              "$(jq -er .server_log_sha256 "$wave_dir/envelope.json")"
            cooperative_transactions=$(rg -c \
              'DeepSeek-V4 cooperative prefill complete' "$server_log" || true)
            test "$cooperative_transactions" = \
              "$(jq -er .cooperative_prefill_transactions "$wave_dir/envelope.json")"
            test "$cooperative_transactions" -gt 0
            decode_cohort_transactions=$(rg -c \
              'DeepSeek-V4 exact decode cohort selected' "$server_log" || true)
            test "$decode_cohort_transactions" = \
              "$(jq -er .decode_cohort_transactions "$wave_dir/envelope.json")"
            test "$decode_cohort_transactions" -gt 0
          done
          check_receipt gemma/lifecycle/summary.json '.receipt_sha256.gemma.lifecycle'
          check_receipt gemma/overlap/summary.json '.receipt_sha256.gemma.overlap'
          check_receipt gemma/wave1/summary.json '.receipt_sha256.gemma.wave1'
          check_receipt gemma/wave2/summary.json '.receipt_sha256.gemma.wave2'
          check_receipt gemma/wave1/thermal/summary.json '.receipt_sha256.gemma.wave1_thermal'
          check_receipt gemma/wave2/thermal/summary.json '.receipt_sha256.gemma.wave2_thermal'
          for wave in 1 2; do
            wave_dir="$gate_dir/gemma/wave$wave"
            bash scripts/verify_gemma4_wave_thermal_receipt.sh "$wave" \
              "$wave_dir/summary.json" "$wave_dir/thermal/summary.json" \
              "$wave_dir/thermal/measurement.log" \
              "$wave_dir/thermal/settle.log" "$wave_dir/agents"
          done
          check_receipt gemma/eight-slots/summary.json '.receipt_sha256.gemma.eight_slots'
          check_receipt gemma/eight-slots/thermal/summary.json '.receipt_sha256.gemma.eight_slots_thermal'
          bash scripts/verify_gemma4_wave_thermal_receipt.sh eight-slots \
            "$gate_dir/gemma/eight-slots/summary.json" \
            "$gate_dir/gemma/eight-slots/thermal/summary.json" \
            "$gate_dir/gemma/eight-slots/thermal/measurement.log" \
            "$gate_dir/gemma/eight-slots/thermal/settle.log" \
            "$gate_dir/gemma/eight-slots/agents"
          jq -e -f scripts/gemma4_eight_slot_receipt.jq \
            "$gate_dir/gemma/eight-slots/summary.json" >/dev/null
          check_receipt gemma/transactions-four-slots.json '.receipt_sha256.gemma.transactions4'
          check_receipt gemma/transactions-eight-slots.json '.receipt_sha256.gemma.transactions8'
          check_receipt gemma/parity/summary.json '.receipt_sha256.gemma.parity'
          bash scripts/verify_gemma4_parity_receipt.sh \
            "$gate_dir/gemma/parity/summary.json" "$gate_dir/gemma/parity"
          check_receipt gemma/heap-summary.json '.receipt_sha256.gemma.heap'
          check_receipt qwen/lifecycle/summary.json '.receipt_sha256.qwen.lifecycle'
          check_receipt qwen/cumulative/cumulative-release-summary.json '.receipt_sha256.qwen.cumulative'
          check_receipt qwen/cancellation/cancellation-summary.json '.receipt_sha256.qwen.cancellation'
          qwen36_validate_cancellation_transaction_counts \
            "$gate_dir/qwen/cancellation/cancellation-summary.json"
          check_receipt qwen38/long-decode/receipt.json '.receipt_sha256.qwen38.long_decode'
          bash scripts/verify_qwen38_long_decode_receipt.sh release \
            "$gate_dir/qwen38/long-decode" "$EXPECTED_SHA" \
            "$(jq -er .crate_sha256 "$manifest")" \
            "$(jq -er .binary_sha256 "$manifest")" \
            "$EXPECTED_QWEN38_MODEL_SHA256"
          for digest in \
            "$EXPECTED_DEEPSEEK_MODEL_SHA256" \
            "$EXPECTED_GEMMA_MODEL_SHA256" \
            "$EXPECTED_QWEN_MODEL_SHA256" \
            "$EXPECTED_QWEN38_MODEL_SHA256"; do
            [[ "$digest" =~ ^[0-9a-f]{64}$ ]]
          done
          jq -e \
            --arg source_sha "$EXPECTED_SHA" \
            --arg deepseek_sha "$EXPECTED_DEEPSEEK_MODEL_SHA256" \
            --arg gemma_sha "$EXPECTED_GEMMA_MODEL_SHA256" \
            --arg qwen_sha "$EXPECTED_QWEN_MODEL_SHA256" \
            --arg qwen38_sha "$EXPECTED_QWEN38_MODEL_SHA256" \
            --arg prompt_contract_sha256 "$prompt_contract_sha" \
            --arg prompt_provenance_sha256 "$prompt_provenance_sha" \
            --slurpfile prompt_contract "$prompt_contract" \
            '. as $root
             | $prompt_contract[0] as $pc
             | .status == "pass"
             and .source_sha == $source_sha
             and .power_guarded_ac == true
             and (.power_event_snapshots_sha256 | test("^[0-9a-f]{64}$"))
             and (.binary_sha256 | test("^[0-9a-f]{64}$"))
             and .dependency_provenance.schema_version == 1
             and .dependency_provenance.status == "pass"
             and .dependency_provenance.package.source == "packed-crate"
             and .dependency_provenance.package.crate_sha256 == $root.crate_sha256
             and .dependency_provenance.build.workspace_ancestry_disjoint == true
             and .dependency_provenance.build.cargo_target_checkout_disjoint == true
             and .dependency_provenance.build.cargo_home_was_fresh_before_resolution == true
             and .dependency_provenance.build.rust_build_override_env_cleared == true
             and .dependency_provenance.dependency.name == "mlx-native"
             and .dependency_provenance.dependency.version == "0.10.16"
             and .dependency_provenance.dependency.requirement == "=0.10.16"
             and .dependency_provenance.dependency.source
               == "registry+https://github.com/rust-lang/crates.io-index"
             and .dependency_provenance.dependency.checksum
               == "9236b563fc45c861ec772cb0b29aafc1144bc49b92b229790c7e1b57844fc83b"
             and .models.deepseek.sha256 == $deepseek_sha
             and .models.gemma.sha256 == $gemma_sha
             and .models.qwen.sha256 == $qwen_sha
             and .models.qwen38.sha256 == $qwen38_sha
             and .fixtures.deepseek_agentic.contract_sha256 == $prompt_contract_sha256
             and .fixtures.deepseek_agentic.prompt_provenance_sha256 == $prompt_provenance_sha256
             and (.families.deepseek as $d
               | $d.status == "pass"
               and $d.cooperative_prefill.schema_version == 2
               and $d.cooperative_prefill.status == "pass"
               and $d.cooperative_prefill.source_sha == $source_sha
               and $d.cooperative_prefill.model_sha256 == $deepseek_sha
               and $d.cooperative_prefill.mlx_native_version == "0.10.16"
               and $d.cooperative_prefill.prefix_rows == 148
               and $d.cooperative_prefill.benchmark.speedup > 1
               and $d.cooperative_prefill.thermal_status == "fair_or_better"
               and $d.cooperative_prefill.required_start_state == "nominal"
               and $d.cooperative_prefill.maximum_measurement_state == "fair"
               and $d.cooperative_prefill.over_limit_measurement_samples == 0
               and $d.cooperative_prefill.non_nominal_measurement_samples
                 == $d.cooperative_prefill.fair_measurement_samples
               and $d.cooperative_prefill.host_contention.policy == "process-group-v1"
               and $d.cooperative_prefill.host_contention.measurement.contended_samples == 0
               and $d.prompt_provenance.status == "pass"
               and $d.prompt_provenance.prompt_contract_sha256 == $prompt_contract_sha256
               and $d.prompt_provenance.model_sha256 == $deepseek_sha
               and ([$d.prompt_provenance.agents[].agent] == [1,2,3,4])
               and $d.lifecycle.status == "pass"
               and $d.lifecycle.active_stream_cancelled_without_done == true
               and $d.lifecycle.queued_exact_retry_cached_tokens > 0
               and $d.lifecycle.unrelated_conversation_content == "ISOLATION_OK"
               and $d.interactive_overlap.status == "pass"
               and $d.interactive_overlap.server.binary_sha256 == $root.binary_sha256
               and $d.interactive_overlap.server.model_sha256 == $deepseek_sha
               and $d.interactive_overlap.server.max_slots == 4
               and $d.interactive_overlap.fixture_sha256 == "3558d4f4b251ed833ee7da1b037fa3f241a4309590d45930b525b690f543a31e"
               and $d.interactive_overlap.long_prompt_tokens > 80000
               and $d.interactive_overlap.first_mixed_prefill_chunk_tokens > 0
               and $d.interactive_overlap.first_mixed_prefill_chunk_tokens <= 256
               and $d.interactive_overlap.first_mixed_prefill_window_cap == 2
               and $d.interactive_overlap.short_generated_tokens_at_first_window >= 8
               and $d.interactive_overlap.terminal_parking.park_observed == true
               and $d.interactive_overlap.terminal_parking.terminal_absent_while_parked == true
               and $d.interactive_overlap.terminal_parking.active_cold_prefills_at_park >= 1
               and $d.interactive_overlap.terminal_parking.park_log_line < $d.interactive_overlap.terminal_parking.long_prefill_complete_log_line
               and $d.interactive_overlap.terminal_parking.long_prefill_complete_log_line < $d.interactive_overlap.terminal_parking.release_log_line
               and $d.interactive_overlap.terminal_parking.release_log_line <= $d.interactive_overlap.terminal_parking.request_complete_log_line
               and $d.interactive_overlap.ready_http == 200
               and $d.interactive_overlap.power_event_delta == 0
               and $d.cached_suffix.status == "pass"
               and $d.cached_suffix.server.binary_sha256 == $root.binary_sha256
               and $d.cached_suffix.overlap.cached_prefill_transactions >= 3
               and $d.cached_suffix.overlap.peer_decode_progress_events_between_transactions >= 1
               and $d.cached_suffix.overlap.peer_terminal_done_exactly_once == true
               and $d.cached_suffix.cancellation.counter_after == ($d.cached_suffix.cancellation.counter_before + 1)
               and $d.cached_suffix.cancellation.chunks_after_stability == $d.cached_suffix.cancellation.chunks_after_settle
               and $d.cached_suffix.cancellation.emitted_done == false
               and (($d.cached_suffix.cancellation.expected_anchor_tokens | type) == "number")
               and $d.cached_suffix.cancellation.expected_anchor_tokens > 0
               and $d.cached_suffix.cancellation.post_cancel_cached_tokens == $d.cached_suffix.cancellation.expected_anchor_tokens
               and $d.cached_suffix.cancellation_after_candidate_anchor.counter_after == ($d.cached_suffix.cancellation_after_candidate_anchor.counter_before + 1)
               and $d.cached_suffix.cancellation_after_candidate_anchor.chunks_after_stability == $d.cached_suffix.cancellation_after_candidate_anchor.chunks_after_settle
               and (($d.cached_suffix.cancellation_after_candidate_anchor.expected_anchor_tokens | type) == "number")
               and $d.cached_suffix.cancellation_after_candidate_anchor.expected_anchor_tokens > 0
               and $d.cached_suffix.cancellation_after_candidate_anchor.post_cancel_cached_tokens == $d.cached_suffix.cancellation_after_candidate_anchor.expected_anchor_tokens
               and $d.cached_suffix.cancellation_during_decode.counter_after == ($d.cached_suffix.cancellation_during_decode.counter_before + 1)
               and $d.cached_suffix.cancellation_during_decode.progress_events_after_stability == $d.cached_suffix.cancellation_during_decode.progress_events_after_settle
               and (($d.cached_suffix.cancellation_during_decode.seed_request_id | type) == "number")
               and $d.cached_suffix.cancellation_during_decode.seed_request_id > 0
               and (($d.cached_suffix.cancellation_during_decode.expected_anchor_tokens | type) == "number")
               and $d.cached_suffix.cancellation_during_decode.expected_anchor_tokens > 0
               and $d.cached_suffix.cancellation_during_decode.post_cancel_cached_tokens == $d.cached_suffix.cancellation_during_decode.expected_anchor_tokens
               and $d.cached_suffix.ready_before == true and $d.cached_suffix.ready_after == true
               and $d.cached_suffix.fatal_log_signatures == 0
               and $d.decode_cohort.schema_version == 3
               and $d.decode_cohort.status == "pass"
               and $d.decode_cohort.source_sha == $root.source_sha
               and $d.decode_cohort.model_sha256 == $deepseek_sha
               and $d.decode_cohort.parity.exact_state_logits_cache_recurrent == true
               and $d.decode_cohort.benchmark.speedup > 1
               and $d.decode_cohort.thermal_status == "fair_or_better"
               and $d.decode_cohort.required_start_state == "nominal"
               and $d.decode_cohort.maximum_measurement_state == "fair"
               and $d.decode_cohort.over_limit_measurement_samples == 0
               and $d.decode_cohort.non_nominal_measurement_samples
                 == $d.decode_cohort.fair_measurement_samples
               and $d.decode_cohort.host_contention.policy == "process-group-v1"
               and $d.decode_cohort.host_contention.measurement.contended_samples == 0
               and ($d.full_context_waves | length) == 2
               and ([$d.full_context_waves[].wave] | sort) == [1, 2]
               and all($d.full_context_waves[];
                 .status == "pass" and .binary_sha256 == $root.binary_sha256
                 and .model_sha256 == $deepseek_sha and .ready_http == 200
                 and .fatal_log_signatures == 0 and .receipt.status == "pass"
                 and (.cooperative_prefill_transactions | type) == "number"
                 and .cooperative_prefill_transactions > 0
                 and (.decode_cohort_transactions | type) == "number"
                 and .decode_cohort_transactions > 0
                 and .receipt.fixture_id == $pc.fixture_id
                 and .receipt.prompt_contract_sha256 == $prompt_contract_sha256
                 and .receipt.prompt_provenance_sha256 == $prompt_provenance_sha256
                 and .receipt.serialization_policy == $pc.serialization.policy
                 and .thermal.schema_version == 2
                 and .thermal.status == "pass"
                 and .thermal.required_state == "nominal"
                 and .thermal.runtime_preflight == "pass"
                 and .thermal.measurement_scope == "cold-cohort"
                 and (.thermal.cold_receipts | type) == "array"
                 and (.thermal.cold_receipts | length) == 4
                 and ([.thermal.cold_receipts[].name] | unique | length) == 4
                 and all(.thermal.cold_receipts[];
                   (.name | test("^agent-[1-4]\\.cold\\.json$"))
                   and (.sha256 | test("^[0-9a-f]{64}$")))
                 and (.thermal.settle_seconds | type) == "number"
                 and .thermal.settle_seconds >= 60
                 and (.thermal.settle_duration_seconds | type) == "number"
                 and .thermal.settle_duration_seconds >= .thermal.settle_seconds
                 and (.thermal.settle_samples | type) == "number"
                 and .thermal.settle_samples > 0
                 and (.thermal.measurement_samples | type) == "number"
                 and .thermal.measurement_samples >= 2
                 and (.thermal.measurement_duration_seconds | type) == "number"
                 and .thermal.measurement_duration_seconds > 0
                 and (.thermal.sample_interval_seconds | type) == "number"
                 and .thermal.sample_interval_seconds == 2
                 and (.thermal.maximum_sample_gap_seconds | type) == "number"
                 and .thermal.maximum_sample_gap_seconds > 0
                 and .thermal.maximum_sample_gap_seconds <= 5
                 and (.thermal.settle_sample_interval_seconds | type) == "number"
                 and .thermal.settle_sample_interval_seconds == 5
                 and (.thermal.maximum_settle_sample_gap_seconds | type) == "number"
                 and .thermal.maximum_settle_sample_gap_seconds > 0
                 and .thermal.maximum_settle_sample_gap_seconds <= 8
                 and (.thermal.non_nominal_measurement_samples | type) == "number"
                 and .thermal.non_nominal_measurement_samples == 0
                 and (.thermal.settle_telemetry_gaps | type) == "number"
                 and .thermal.settle_telemetry_gaps == 0
                 and (.thermal.telemetry_gaps | type) == "number"
                 and .thermal.telemetry_gaps == 0
                 and (.thermal.settle_log_sha256 | test("^[0-9a-f]{64}$"))
                 and (.thermal.measurement_log_sha256 | test("^[0-9a-f]{64}$"))
                 and .thermal.host_contention.policy == "process-group-v1"
                 and .thermal.host_contention.measurement.contended_samples == 0
                 and (.cold_prefill_tokens_per_second | type) == "array"
                 and (.cold_prefill_tokens_per_second | length) == 4
                 and all(.cold_prefill_tokens_per_second[]; type == "number" and . > 0)
                 and .receipt.family == "deepseek4" and .receipt.concurrent_agents == 4
                 and .receipt.require_cold_first == 1 and (.receipt.agents | length) == 4
                 and .receipt.agentic_context_fixture_sha256 == $pc.repository_context.sha256
                 and .receipt.agentic_context_fixture_bytes == $pc.repository_context.bytes
                 and .receipt.repository_context_chars == $pc.repository_context.chars
                 and .receipt.prompt_tokens == $pc.serialization.expected_prompt_tokens
                 and .receipt.prompt_tokens != $pc.serialization.legacy_rejected_prompt_tokens
                 and all(.receipt.agents[];
                   .status == "pass" and .cold_cached_tokens == 0
                   and .agentic_context_fixture_sha256 == $pc.repository_context.sha256
                   and .agentic_context_fixture_bytes == $pc.repository_context.bytes
                   and .repository_context_chars == $pc.repository_context.chars
                   and .expected_path == $pc.request.expected_path
                   and .prompt_tokens == $pc.serialization.expected_prompt_tokens
                   and .prompt_tokens != $pc.serialization.legacy_rejected_prompt_tokens
                   and .cold_ttft_ms <= 60000
                   and .cold_semantic_response_ms <= 60000
                   and .cached_tokens == $pc.prompt.cached_anchor_tokens
                   and .auto_cached_tokens == $pc.prompt.cached_anchor_tokens
                   and .stream_cached_tokens == $pc.prompt.cached_anchor_tokens
                   and .continuation_cached_tokens == $pc.prompt.cached_anchor_tokens
                   and .continuation_uncached_tokens == $pc.prompt.tool_result_uncached_suffix_tokens
                   and .cached_ttft_ms <= 5000
                   and .cached_semantic_response_ms <= 15000
                   and .auto_semantic_response_ms <= 15000
                   and .cached_sse_tool_call_ms <= 15000
                   and .tool_result_response_ms <= 35000)))
             and (.families.gemma as $g
               | $g.status == "pass" and $g.lifecycle.status == "pass"
               and $g.lifecycle.active_stream_cancelled_without_done == true
               and $g.lifecycle.queued_exact_retry_cached_tokens > 0
               and $g.lifecycle.unrelated_conversation_content == "ISOLATION_OK"
               and $g.overlap_and_cancellation.status == "pass"
               and $g.overlap_and_cancellation.binary_sha256 == $root.binary_sha256
               and $g.overlap_and_cancellation.model_sha256 == $gemma_sha
               and $g.overlap_and_cancellation.max_slots == 4
               and $g.overlap_and_cancellation.primary_context_sha256 == "07b147e9c6ac26a0c9c4a719391c0772b2d27b9d77499479014b9ace88b6b11e"
               and $g.overlap_and_cancellation.cancellation_context_sha256 == "f0b264eedae315618941d8fa6fb16454c4eac03b5793e213b613d66ccb7b6e4a"
               and $g.overlap_and_cancellation.long_prompt_tokens > 80000
               and $g.overlap_and_cancellation.short_semantic_during_long_prefill == true
               and $g.overlap_and_cancellation.committed_tokens_when_short_progressed < $g.overlap_and_cancellation.long_prompt_tokens
               and $g.overlap_and_cancellation.cancellation_delta == 1
               and $g.overlap_and_cancellation.chunks_after_cancel == $g.overlap_and_cancellation.chunks_after_stability
               and $g.overlap_and_cancellation.rollback_restores >= 1
               and $g.overlap_and_cancellation.transaction_cap_tokens == 4096
               and $g.overlap_and_cancellation.ready_http == 200
               and $g.overlap_and_cancellation.power_event_delta == 0
               and ($g.agent_waves | length) == 3
               and ($g.agent_waves | map(.concurrent_agents) | sort) == [4,4,8]
               and ([$g.agent_waves[] | select(.concurrent_agents == 4) | .wave_id] | sort) == ["wave1","wave2"]
               and all($g.agent_waves[];
                 .status == "pass" and .family == "gemma4" and .require_cold_first == 1
                 and all(.agents[]; .status == "pass" and .cold_cached_tokens == 0
                   and .cached_tokens >= (.prompt_tokens - 32)
                   and .auto_cached_tokens >= (.prompt_tokens - 32)
                   and .continuation_cached_tokens >= (.prompt_tokens - 32)))
               and all($g.agent_waves[] | select(.concurrent_agents == 4);
                 (.wave_id == "wave1" or .wave_id == "wave2")
                 and .thermal.status == "pass"
                 and .thermal.concurrent_agents == 4
                 and .thermal.required_state == "nominal"
                 and .thermal.measurement_scope == "full-agent-wave"
                 and .thermal.settle_seconds == 60
                 and .thermal.settle_duration_seconds >= 60
                 and .thermal.settle_samples > 0
                 and .thermal.measurement_samples >= 2
                 and .thermal.measurement_duration_seconds > 0
                 and .thermal.sample_interval_seconds == 2
                 and .thermal.maximum_sample_gap_seconds == 5
                 and .thermal.settle_sample_interval_seconds == 5
                 and .thermal.maximum_settle_sample_gap_seconds == 8
                 and .thermal.non_nominal_measurement_samples == 0
                 and .thermal.settle_telemetry_gaps == 0
                 and .thermal.telemetry_gaps == 0
                 and (.thermal.cold_receipts | length) == 4)
               and all($g.agent_waves[] | select(.concurrent_agents == 8);
                 .wave_id == "eight-slots"
                 and (.agents | type) == "array" and (.agents | length) == 8
                 and .thermal.status == "pass"
                 and .thermal.phase == "gemma-eight-slots"
                 and .thermal.concurrent_agents == 8
                 and .thermal.required_state == "nominal"
                 and .thermal.measurement_scope == "full-agent-wave"
                 and .thermal.settle_seconds == 60
                 and .thermal.settle_duration_seconds >= 60
                 and .thermal.settle_samples > 0
                 and .thermal.measurement_samples >= 2
                 and .thermal.measurement_duration_seconds > 0
                 and .thermal.sample_interval_seconds == 2
                 and .thermal.maximum_sample_gap_seconds == 5
                 and .thermal.settle_sample_interval_seconds == 5
                 and .thermal.maximum_settle_sample_gap_seconds == 8
                 and .thermal.non_nominal_measurement_samples == 0
                 and .thermal.settle_telemetry_gaps == 0
                 and .thermal.telemetry_gaps == 0
                 and (.thermal.cold_receipts | length) == 8
                 and (.maximum_cold_ttft_ms | type) == "number"
                 and .maximum_cold_ttft_ms >= 0 and .maximum_cold_ttft_ms <= 40000
                 and (.maximum_cold_semantic_response_ms | type) == "number"
                 and .maximum_cold_semantic_response_ms >= 0
                 and .maximum_cold_semantic_response_ms <= 60000
                 and (.maximum_tool_result_ms | type) == "number"
                 and .maximum_tool_result_ms >= 0 and .maximum_tool_result_ms <= 30000
                 and all(.agents[];
                   (.cold_ttft_ms | type) == "number"
                   and .cold_ttft_ms >= 0 and .cold_ttft_ms <= 40000
                   and (.cold_semantic_response_ms | type) == "number"
                   and .cold_semantic_response_ms >= 0
                   and .cold_semantic_response_ms <= 60000
                   and (.tool_result_response_ms | type) == "number"
                   and .tool_result_response_ms >= 0
                   and .tool_result_response_ms <= 30000)
                 and .maximum_cold_ttft_ms == ([.agents[].cold_ttft_ms] | max)
                 and .maximum_cold_semantic_response_ms == ([.agents[].cold_semantic_response_ms] | max)
                 and .maximum_tool_result_ms == ([.agents[].tool_result_response_ms] | max))
               and ($g.transactions | length) == 2
               and all($g.transactions[]; .status == "pass" and .transaction_cap_rows == 4096
                 and .max_transaction_rows <= 4096 and .multi_slot_transactions > 0
                 and .nonaligned_transactions > 0)
               and $g.parity.status == "pass"
               and $g.parity.profile == "release"
               and $g.parity.n4_exact_output_parity == true
               and $g.parity.n8_exact_output_parity == true
               and $g.parity.n8_cross_slot_admit == true
               and $g.parity.n8_max_tokens == 24
               and $g.parity.n8_rounds == 25
               and $g.parity.n8_seed_budget_exact_output_parity == true
               and $g.parity.n8_seed_budget_max_tokens == 1
               and $g.parity.n8_seed_budget_rounds == 25
               and $g.parity.n8_tiny_hybrid_exact_output_parity == true
               and $g.parity.n8_tiny_full_tq_exact_output_parity == true
               and $g.parity.n8_tiny_prefill_rounds == 64
               and $g.parity.n8_tiny_resume_rounds == 16
               and $g.parity.fresh_and_reused_4096_8193_bounded_output_parity == true
               and $g.parity.long_resume_exact_output_parity == true
               and $g.heap.status == "pass"
               and $g.heap.snapshot_order == ["baseline","post_wave1","post_wave2","post_overlap","post_lifecycle"]
               and all($g.heap.snapshots[]; .command_buffer_objects == 0 and .command_buffer_impls == 0)
               and all($g.heap.snapshots[];
                 (.cfstring_count | type) == "number" and .cfstring_count >= 0
                 and (.autoreleasepool_content_count | type) == "number"
                 and .autoreleasepool_content_count >= 0)
               and ($g.heap.snapshots.post_wave1.cfstring_count - $g.heap.snapshots.baseline.cfstring_count) <= 256
               and ($g.heap.snapshots.post_wave2.cfstring_count - $g.heap.snapshots.post_wave1.cfstring_count) <= 256
               and ($g.heap.snapshots.post_wave2.cfstring_count - $g.heap.snapshots.baseline.cfstring_count) <= 512
               and ($g.heap.snapshots.post_overlap.cfstring_count - $g.heap.snapshots.post_wave2.cfstring_count) <= 512
               and ($g.heap.snapshots.post_lifecycle.cfstring_count - $g.heap.snapshots.post_overlap.cfstring_count) <= 512
               and ($g.heap.snapshots.post_lifecycle.cfstring_count - $g.heap.snapshots.post_wave2.cfstring_count) <= 1024
               and ($g.heap.snapshots.post_wave1.autoreleasepool_content_count - $g.heap.snapshots.baseline.autoreleasepool_content_count) <= 8
               and ($g.heap.snapshots.post_wave2.autoreleasepool_content_count - $g.heap.snapshots.post_wave1.autoreleasepool_content_count) <= 8
               and ($g.heap.snapshots.post_wave2.autoreleasepool_content_count - $g.heap.snapshots.baseline.autoreleasepool_content_count) <= 16
               and ($g.heap.snapshots.post_overlap.autoreleasepool_content_count - $g.heap.snapshots.post_wave2.autoreleasepool_content_count) <= 8
               and ($g.heap.snapshots.post_lifecycle.autoreleasepool_content_count - $g.heap.snapshots.post_overlap.autoreleasepool_content_count) <= 8
               and ($g.heap.snapshots.post_lifecycle.autoreleasepool_content_count - $g.heap.snapshots.post_wave2.autoreleasepool_content_count) <= 16)
             and (.families.qwen as $q
               | $q.status == "pass" and $q.lifecycle.status == "pass"
               and $q.lifecycle.active_stream_cancelled_without_done == true
               and $q.lifecycle.queued_exact_retry_cached_tokens > 0
               and $q.lifecycle.unrelated_conversation_content == "ISOLATION_OK"
               and $q.cumulative.status == "pass"
               and $q.cumulative.binary_sha256 == $root.binary_sha256
               and $q.cumulative.model_sha256 == $qwen_sha
               and $q.cumulative.max_slots == 4 and $q.cumulative.ready_http == 200
               and $q.cumulative.power_event_delta == 0 and $q.cumulative.heap_bounds_valid == true
               and $q.cumulative.overlap.prompt_tokens == 87972
               and $q.cumulative.overlap.short_prompt_tokens == 552
               and $q.cumulative.overlap.tools == 347
               and $q.cumulative.overlap.chunks == 44
               and $q.cumulative.overlap.full_chunks == 42
               and $q.cumulative.overlap.short_semantic_before_long_chunk == true
               and $q.cumulative.overlap.continuation_cached_tokens > 80000
               and $q.cumulative.overlap.continuation_first_chunk_start == $q.cumulative.overlap.continuation_cached_tokens
               and all($q.cumulative.agent_waves[];
                 .status == "pass" and .family == "qwen36" and .concurrent_agents == 4
                 and .require_cold_first == 1
                 and .agentic_system_prompt_sha256 == "dbedef4c2efa20e51603355def14af7f11658a03fd0dcda6de9d900588f7cb3c"
                 and .tool_result_success_prefix_sha256 == "2d73b080a69f15d8cac736b264d82fdb4e8fe40e6cff6ed6bf099565f499ef5a"
                 and all(.agents[];
                   .cold_cached_tokens == 0
                   and .expected_path == "/opt/hf2q/Cargo.toml"
                   and .agentic_system_prompt_sha256 == "dbedef4c2efa20e51603355def14af7f11658a03fd0dcda6de9d900588f7cb3c"
                   and .tool_result_success_prefix_sha256 == "2d73b080a69f15d8cac736b264d82fdb4e8fe40e6cff6ed6bf099565f499ef5a"))
               and $q.cumulative.agent_waves.warmup.maximum_tool_result_ms <= 15000
               and $q.cumulative.agent_waves.wave1.maximum_tool_result_ms <= 10000
               and $q.cumulative.agent_waves.wave2.maximum_tool_result_ms <= 10000
               and all($q.cumulative.heap[]; .command_buffer_objects == 0 and .command_buffer_impls == 0)
               and $q.cumulative.heap_deltas.cfstring_baseline_to_warmup <= 1024
               and $q.cumulative.heap_deltas.cfstring_warmup_to_wave2 <= 512
               and $q.cumulative.heap_deltas.pool_baseline_to_warmup <= 16
               and $q.cumulative.heap_deltas.pool_warmup_to_wave2 <= 16
               and $q.cancellation.status == "pass"
               and $q.cancellation.binary_sha256 == $root.binary_sha256
               and $q.cancellation.model_sha256 == $qwen_sha
               and $q.cancellation.max_slots == 1
               and ($q.cancellation.chunks_at_disconnect | type) == "number"
               and ($q.cancellation.chunks_after_cancel | type) == "number"
               and ($q.cancellation.chunks_after_stability | type) == "number"
               and $q.cancellation.chunks_at_disconnect == ($q.cancellation.chunks_at_disconnect | floor)
               and $q.cancellation.chunks_after_cancel == ($q.cancellation.chunks_after_cancel | floor)
               and $q.cancellation.chunks_after_stability == ($q.cancellation.chunks_after_stability | floor)
               and $q.cancellation.chunks_at_disconnect == 3
               and $q.cancellation.chunks_after_cancel >= $q.cancellation.chunks_at_disconnect
               and $q.cancellation.chunks_after_cancel <= ($q.cancellation.chunks_at_disconnect + 1)
               and $q.cancellation.cancellation_delta == 1
               and $q.cancellation.cancelled_success_terminal == false
               and $q.cancellation.same_slot_reuse == true
               and $q.cancellation.chunks_after_cancel == $q.cancellation.chunks_after_stability
               and $q.cancellation.pre_tiny_ready_http == 200
               and $q.cancellation.ready_http == 200
               and $q.cancellation.power_event_delta == 0)
             and (.families.qwen38 as $q38
               | $q38.status == "pass"
               and $q38.long_decode.schema_version == 1
               and $q38.long_decode.status == "pass"
               and $q38.long_decode.benchmark.schema_version == 2
               and $q38.long_decode.benchmark.status == "pass"
               and $q38.long_decode.benchmark.identity.source_sha == $source_sha
               and $q38.long_decode.benchmark.identity.crate_sha256 == $root.crate_sha256
               and $q38.long_decode.benchmark.identity.binary.sha256 == $root.binary_sha256
               and $q38.long_decode.benchmark.identity.model.sha256 == $qwen38_sha
               and $q38.long_decode.benchmark.trial_order == ["off","auto","auto","off"]
               and $q38.long_decode.benchmark.settings.temperature == 0
               and ($q38.long_decode.benchmark.settings | has("seed") | not)
               and $q38.long_decode.benchmark.aggregate.improvement_percent >= 15
               and ($q38.long_decode.benchmark.aggregate.short_regression_percent
                 | type) == "number"
               and $q38.long_decode.benchmark.aggregate.short_regression_percent <= 2
               and ($q38.long_decode.benchmark.aggregate.short_exact_output_sha256
                 | test("^[0-9a-f]{64}$"))
               and $q38.long_decode.thermal.status == "pass"
               and $q38.long_decode.thermal.required_start_state == "nominal"
               and $q38.long_decode.thermal.maximum_measurement_state == "fair"
               and $q38.long_decode.thermal.over_limit_measurement_samples == 0
               and $q38.long_decode.thermal.measurement_scope == "full-abba-benchmark")' \
            "$manifest" >/dev/null
          expected_crate_sha=$(jq -er '.crate_sha256' "$manifest")
          [[ "$expected_crate_sha" =~ ^[0-9a-f]{64}$ ]]
          standalone_dir="$gate_dir/standalone"
          for required in \
            hf2q-aarch64-apple-darwin \
            hf2q-aarch64-apple-darwin.sha256 \
            proof.json \
            notary-submission.json \
            notary-wait.json \
            notary-log.json \
            codesign.txt \
            gatekeeper.txt; do
            test -f "$standalone_dir/$required"
            test ! -L "$standalone_dir/$required"
          done
          test "$(find "$standalone_dir" -mindepth 1 -maxdepth 1 -print | wc -l | tr -d ' ')" = 8
          standalone_proof="$standalone_dir/proof.json"
          standalone_binary="$standalone_dir/hf2q-aarch64-apple-darwin"
          chmod 0555 "$standalone_binary"
          standalone_sha=$(jq -er .asset.sha256 "$standalone_proof")
          standalone_size=$(jq -er .asset.size "$standalone_proof")
          standalone_team=$(jq -er .signing.team_id "$standalone_proof")
          standalone_identifier=$(jq -er .signing.identifier "$standalone_proof")
          jq -e \
            --arg source_sha "$EXPECTED_SHA" \
            --arg version "$EXPECTED_VERSION" \
            --arg binary_sha "$(jq -er .binary_sha256 "$manifest")" '
              .kind == "hf2q.standalone-apple-release-proof"
              and .schema_version == 1
              and .package == "hf2q"
              and .source_sha == $source_sha
              and .version == $version
              and .target == "aarch64-apple-darwin"
              and .asset.name == "hf2q-aarch64-apple-darwin"
              and .asset.sha256 == $binary_sha
              and (.asset.size | type) == "number"
              and .asset.size > 0
              and (.signing.team_id | test("^[A-Z0-9]{10}$"))
              and .signing.identifier == "us.hf2q.cli"
              and .signing.hardened_runtime == true
              and .signing.secure_timestamp == true
              and .notarization.status == "Accepted"
              and .notarization.standalone_ticket_stapled == false
              and .verification.codesign == "accepted"
              and .verification.gatekeeper == "Notarized Developer ID"
            ' "$standalone_proof" >/dev/null
          for pair in \
            "notary-submission.json:.notarization.submission_sha256" \
            "notary-wait.json:.notarization.wait_sha256" \
            "notary-log.json:.notarization.log_sha256" \
            "codesign.txt:.verification.codesign_log_sha256" \
            "gatekeeper.txt:.verification.gatekeeper_log_sha256"; do
            file=${pair%%:*}
            selector=${pair#*:}
            test "$(shasum -a 256 "$standalone_dir/$file" | awk '{print $1}')" = \
              "$(jq -er "$selector" "$standalone_proof")"
          done
          test "$(shasum -a 256 "$standalone_binary" | awk '{print $1}')" = "$standalone_sha"
          test "$(stat -f '%z' "$standalone_binary")" = "$standalone_size"
          test "$(awk 'NF {print $1}' "$standalone_dir/hf2q-aarch64-apple-darwin.sha256")" = \
            "$standalone_sha"
          test "$(awk 'NF {print $2}' "$standalone_dir/hf2q-aarch64-apple-darwin.sha256")" = \
            hf2q-aarch64-apple-darwin
          test "$(/usr/bin/lipo -archs "$standalone_binary")" = arm64
          test "$(/usr/bin/vtool -show-build "$standalone_binary" | awk '$1 == "minos" {print $2}')" = 14.0
          /usr/bin/codesign --verify --strict --all-architectures "$standalone_binary"
          /usr/sbin/spctl --assess --type execute "$standalone_binary"
          test "$(HF2Q_NO_COMPLETION_INSTALL=1 "$standalone_binary" --version)" = \
            "hf2q $EXPECTED_VERSION"
          echo "EXPECTED_CRATE_SHA256=$expected_crate_sha" >> "$GITHUB_ENV"
          {
            printf 'STANDALONE_RELEASE_DIR=%s\n' "$standalone_dir"
            printf 'STANDALONE_BINARY_SHA256=%s\n' "$standalone_sha"
            printf 'STANDALONE_BINARY_SIZE=%s\n' "$standalone_size"
            printf 'STANDALONE_TEAM_ID=%s\n' "$standalone_team"
            printf 'STANDALONE_IDENTIFIER=%s\n' "$standalone_identifier"
          } >> "$GITHUB_ENV"

      - name: Toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # master
        with:
          toolchain: "1.88.0"

      - name: cargo audit
        shell: bash
        run: |
          cargo install cargo-audit --locked --version 0.22.2 --no-default-features
          cargo audit

      - name: Package exact source
        shell: bash
        run: |
          set -euo pipefail
          if [[ ${MLX_NATIVE_SKIP_METALLIB+x} ]]; then
            echo "MLX_NATIVE_SKIP_METALLIB is forbidden for release builds" >&2
            exit 1
          fi
          cargo package --locked --no-verify
          crate="target/package/hf2q-${EXPECTED_VERSION}.crate"
          test -s "$crate"
          actual_crate_sha=$(shasum -a 256 "$crate" | awk '{print $1}')
          test "$actual_crate_sha" = "$EXPECTED_CRATE_SHA256"
          release_package_stage=$(mktemp -d \
            "$RUNNER_TEMP/hf2q-release-package.XXXXXX")
          /usr/bin/tar -xzf "$crate" -C "$release_package_stage"
          package_root=$(cd \
            "$release_package_stage/hf2q-${EXPECTED_VERSION}" && pwd -P)
          packed_lock="$package_root/Cargo.lock"
          bash scripts/verify_release_dependency_provenance.sh \
            check-package-root "$package_root"
          bash scripts/verify_release_dependency_provenance.sh verify \
            "$DEPENDENCY_PROVENANCE_DIR" "$packed_lock"
          echo "RELEASE_PACKAGE_ROOT=$package_root" >> "$GITHUB_ENV"
          printf '%s  %s\n' "$actual_crate_sha" "$crate" \
            | tee "$RUNNER_TEMP/release-crate.sha256"

      - name: Install and smoke-test packed artifact
        shell: bash
        run: |
          set -euo pipefail
          package_root=${RELEASE_PACKAGE_ROOT:?RELEASE_PACKAGE_ROOT is required}
          (
            cd "$package_root"
            cargo check --locked --all-targets --all-features
            cargo test --locked --lib --all-features
            cargo test --locked --test convert_integration --all-features
            cargo test --locked --test lcp_registry_unit --all-features
            cargo test --locked --bin hf2q --all-features convert::orchestrator::tests::
            cargo test --locked --bin hf2q --all-features distribution::schema::
            cargo test --locked --bin hf2q --all-features distribution::install_state:: -- --test-threads=1
            cargo test --locked --bin hf2q --all-features distribution::update_auth:: -- --test-threads=1
            cargo test --locked --bin hf2q --all-features distribution::update_transport:: -- --test-threads=1
            cargo test --locked --bin hf2q --all-features distribution::prepared_release:: -- --test-threads=1
            cargo test --locked --bin hf2q --all-features distribution::standalone:: -- --test-threads=1
            scripts/test_standalone_installer.sh
            scripts/test_standalone_release_signing_contract.sh
            cargo test --locked --bin hf2q --all-features setup:: -- --test-threads=1
            cargo test --locked --bin hf2q --all-features tests::convert_config_quant_is_used_and_explicit_quant_wins -- --exact
            cargo test --locked --bin hf2q --all-features tests::invalid_operator_config_fails_before_convert_source_or_serve_bind -- --exact
            cargo test --locked --bin hf2q --all-features serve::tests::serve_config_defaults_are_used_and_cli_env_override_them -- --exact
            cargo test --locked --bin hf2q --all-features serve::tests::configured_lan_requires_runtime_auth -- --exact
            cargo test --locked --bin hf2q --all-features serve::tests::c4_scheduler_cli_wins_over_env -- --exact
            cargo test --locked --bin hf2q --all-features serve::tests::c4_max_slots_cli_wins_over_env -- --exact
            direct_metadata="$(cargo +1.88.0 metadata --locked --all-features --format-version 1 --no-deps)"
            jq -e '
              .packages[]
              | select(.name == "hf2q")
              | .dependencies[]
              | select(.name == "sigstore-tuf")
              | .req == "=0.11.0"
                and .uses_default_features == false
                and .features == []
            ' <<<"$direct_metadata"
            jq -e '
              .packages[]
              | select(.name == "hf2q")
              | .dependencies[]
              | select(.name == "toml")
              | .req == "=1.1.4"
                and .uses_default_features == false
                and (.features | sort) == ["display", "parse", "serde"]
            ' <<<"$direct_metadata"
            jq -e '
              [.packages[]
                | select(.name == "hf2q")
                | .dependencies[]
                | select(
                    .name == "percent-encoding"
                    or .name == "sha1"
                    or .name == "url"
                  )
                | {name, req, uses_default_features, features}]
              | sort_by(.name)
              == [
                {"name":"percent-encoding","req":"=2.3.2","uses_default_features":true,"features":[]},
                {"name":"sha1","req":"=0.10.7","uses_default_features":true,"features":[]},
                {"name":"url","req":"=2.5.8","uses_default_features":true,"features":[]}
              ]
            ' <<<"$direct_metadata"
            jq -e '
              .packages[]
              | select(.name == "hf2q")
              | .dependencies[]
              | select(.name == "flate2")
              | .req == "=1.1.9"
                and .uses_default_features == false
                and .features == ["zlib-rs"]
            ' <<<"$direct_metadata"
            jq -e '
              .packages[]
              | select(.name == "hf2q")
              | .dependencies[]
              | select(.name == "zip")
              | .req == "=7.2.0"
                and .uses_default_features == false
                and .features == ["deflate-flate2-zlib-rs"]
            ' <<<"$direct_metadata"
            jq -e '
              [.packages[]
                | select(.name == "hf2q")
                | .dependencies[]
                | select(
                    .name == "core-foundation"
                    or .name == "security-framework"
                    or .name == "security-framework-sys"
                  )
                | {
                    name,
                    req,
                    uses_default_features,
                    features,
                    target
                  }]
              | sort_by(.name)
              == [
                {
                  "name":"core-foundation",
                  "req":"=0.10.1",
                  "uses_default_features":true,
                  "features":[],
                  "target":"cfg(target_os = \"macos\")"
                },
                {
                  "name":"security-framework",
                  "req":"=3.7.0",
                  "uses_default_features":false,
                  "features":["macos-12"],
                  "target":"cfg(target_os = \"macos\")"
                },
                {
                  "name":"security-framework-sys",
                  "req":"=2.17.0",
                  "uses_default_features":false,
                  "features":["macos-12"],
                  "target":"cfg(target_os = \"macos\")"
                }
              ]
            ' <<<"$direct_metadata"
            jq -e '
              .packages[]
              | select(.name == "hf2q")
              | .dependencies[]
              | select(.name == "sysctl")
              | .req == "=0.6.0"
                and .uses_default_features == false
                and .features == []
                and .target == "cfg(target_os = \"macos\")"
            ' <<<"$direct_metadata"
            resolved_metadata="$(cargo +1.88.0 metadata --locked --all-features --format-version 1)"
            jq -e '
              (.packages[] | select(.name == "toml") | .id) as $id
              | .resolve.nodes[]
              | select(.id == $id)
              | (.features | sort) == ["display", "parse", "serde"]
            ' <<<"$resolved_metadata"
            jq -e '
              (.packages[]
                | select(.name == "sigstore-tuf" and .version == "0.11.0")
                | .id) as $id
              | .resolve.nodes[]
              | select(.id == $id)
              | .features == []
            ' <<<"$resolved_metadata"
            jq -e '
              [.packages[]
                  | select(
                      (.name == "percent-encoding" and .version == "2.3.2")
                      or (.name == "sha1" and .version == "0.10.7")
                      or (.name == "url" and .version == "2.5.8")
                    )
                  | .id] as $ids
              | [.resolve.nodes[]
                  | select(.id as $id | $ids | index($id))
                  | ((.id | split("#")[-1]) + ":" + ((.features | sort) | join(",")))]
                | sort
                == [
                  "percent-encoding@2.3.2:alloc,default,std",
                  "sha1@0.10.7:default,std",
                  "url@2.5.8:default,std"
                ]
            ' <<<"$resolved_metadata"
            jq -e '
              (.packages[]
                | select(.name == "zip" and .version == "7.2.0")
                | .id) as $id
              | .resolve.nodes[]
              | select(.id == $id)
              | (.features | sort) == [
                  "_deflate-any",
                  "deflate-flate2",
                  "deflate-flate2-zlib-rs"
                ]
            ' <<<"$resolved_metadata"
            jq -e '
              (.packages[]
                | select(.name == "flate2" and .version == "1.1.9")
                | .id) as $id
              | .resolve.nodes[]
              | select(.id == $id)
              | (.features | sort) == [
                  "any_impl",
                  "any_zlib",
                  "default",
                  "miniz_oxide",
                  "rust_backend",
                  "zlib-rs"
                ]
            ' <<<"$resolved_metadata"
            jq -e '[.packages[] | select(.name == "tough")] | length == 0' \
              <<<"$resolved_metadata"
            unexpected_imports="$(
              grep -RIl --include='*.rs' 'sigstore_tuf' src \
                | grep -v '^src/distribution/update_auth/' || true
            )"
            test -z "$unexpected_imports"
            unexpected_transport_authority="$(
              grep -RInE --include='*.rs' \
                '^[[:space:]]*use[[:space:]]+(reqwest|hyper|std::net|tokio::net|url::)|std::process::Command|clap::' \
                src/distribution/update_auth || true
            )"
            test -z "$unexpected_transport_authority"
            unexpected_metadata_session_callers="$(
              grep -RIlE --include='*.rs' \
                'begin_metadata_update|MetadataFetchResponse|MetadataSessionProgress|MetadataUpdateSession' \
                src/distribution \
                | grep -v '^src/distribution/update_auth/' \
                | grep -v '^src/distribution/update_transport/' || true
            )"
            test -z "$unexpected_metadata_session_callers"
            unexpected_preparation_authority="$(
              grep -RInE --include='*.rs' --exclude='tests.rs' \
                'rustix::fs|std::fs|File::(create|open|options)|OpenOptions::|std::net|std::process|reqwest|hyper|tokio::net|(^|[^:[:alnum:]_])url::|clap::|ZipWriter|\.extract(_unwrapped_root_dir)?\(' \
                src/distribution/prepared_release || true
            )"
            unexpected_preparation_authority="$(
              grep -vE '^[^:]+:[0-9]+:use std::fs::File;$' \
                <<<"$unexpected_preparation_authority" || true
            )"
            test -z "$unexpected_preparation_authority"
            unexpected_extraction_authority="$(
              grep -InE \
                'remove_named|remove_dir|remove_file|unlink|rename|fchmod|AuthenticatedPreparedVersion|version-installation|install-receipt|std::process::Command|reqwest|hyper|tokio::net|clap::' \
                src/distribution/install_state/extraction.rs \
                src/distribution/install_state/extraction/io.rs \
                src/distribution/install_state/extraction/modes.rs \
                src/distribution/prepared_release/extract.rs || true
            )"
            test -z "$unexpected_extraction_authority"
            unexpected_identity_deletion="$(
              grep -RInE --include='*.rs' \
                'remove_named|remove_dir|remove_file|unlink|quarantine|trash' \
                src/distribution/install_state/identity.rs \
                src/distribution/install_state/identity || true
            )"
            test -z "$unexpected_identity_deletion"
            test "$(grep -Fc 'open_private_regular_file_read_only(&update, LOCK_FILE)' src/distribution/install_state/identity.rs)" = 1
            test "$(grep -Fc 'pub(super) fn open_private_regular_file_read_only' src/distribution/install_state/unix.rs)" = 1
            unexpected_transport_extraction="$(
              grep -RInE --include='*.rs' --exclude='tests.rs' \
                'ReleaseExtractionStage|PostReplayExtraction|ExtractedRelease|extract_release|with_locked_extraction' \
                src/distribution/update_transport || true
            )"
            test -z "$unexpected_transport_extraction"
            unexpected_hf_external_authority="$(
              grep -InE \
                'std::process|Command::new|(^|[^[:alnum:]_])reqwest(::|[[:space:]])|(^|[^[:alnum:]_])hyper(::|[[:space:]])' \
                src/input/hf_reference.rs \
                src/input/hf_download.rs \
                src/input/hf_download/resolution.rs \
                src/input/integrity.rs \
                src/convert/receipt.rs \
                src/main.rs || true
            )"
            unexpected_hf_external_authority="$(
              grep -vE '^src/main.rs:[0-9]+:use std::process::ExitCode;$' \
                <<<"$unexpected_hf_external_authority" || true
            )"
            test -z "$unexpected_hf_external_authority"
            unexpected_pretransfer_authority="$(
              grep -nE \
                'repo\.(get|download|download_with_progress)\(|download_file|std::fs|File::|OpenOptions|std::process|Command::new|reqwest|hyper|tokio::net|clap::|Serialize|Deserialize' \
                src/input/hf_download/resolution.rs || true
            )"
            test -z "$unexpected_pretransfer_authority"
            unexpected_payload_authority="$(
              grep -nE \
                'std::fs::(remove_dir|remove_file|rename|copy|write|set_permissions)|File::create|OpenOptions|verify_conversion_manifest\(|verify_source\(|convert::|quantize|std::process|Command::new|reqwest|hyper|tokio::net|clap::|Serialize|Deserialize' \
                src/input/hf_download/resolution/payload.rs || true
            )"
            test -z "$unexpected_payload_authority"
            unexpected_source_auth_authority="$(
              grep -nE \
                'build_hub_api|download_file|ApiBuilder|repo\.(get|download|download_with_progress)\(|std::fs::(create_dir|remove_dir|remove_file|rename|copy|write|set_permissions)|File::create|OpenOptions|convert::|quantize|std::process|Command::new|reqwest|hyper|tokio::net|clap::|Serialize|Deserialize' \
                src/input/hf_download/resolution/payload/source.rs || true
            )"
            test -z "$unexpected_source_auth_authority"
            unexpected_recipe_conversion_authority="$(
              grep -nE \
                'build_hub_api|download_file|ApiBuilder|repo\.(get|download|download_with_progress)\(|std::fs::(remove_dir|remove_file|rename|copy|write|set_permissions)|std::process|Command::new|reqwest|hyper|tokio::net|clap::|profile_path\(|preparation_receipt_path\(|SourceRetentionChoice|serve::|calibration::' \
                src/input/hf_download/resolution/payload/source/conversion.rs || true
            )"
            test -z "$unexpected_recipe_conversion_authority"
            unexpected_recipe_external_authority="$(
              grep -RInE --include='*.rs' \
                'hf_hub|resolve_model_reference|resolve_model_preparation_plan|authorize_model_preparation_transfer|std::process|Command::new|reqwest|hyper|tokio::net|clap::' \
                src/input/model_recipe.rs src/input/model_recipe || true
            )"
            test -z "$unexpected_recipe_external_authority"
            unexpected_recipe_plan_authority="$(
              grep -nE \
                'std::fs::(write|copy|create_dir|remove_dir|remove_file|rename|set_permissions)|File::create|OpenOptions|std::process|Command::new|reqwest|hyper|tokio::net|clap::|Serialize|Deserialize' \
                src/input/model_recipe/plan.rs || true
            )"
            test -z "$unexpected_recipe_plan_authority"
            unexpected_prepared_profile_authority="$(
              grep -nE \
                'std::fs|File::|OpenOptions|hf_hub|std::process|Command::new|reqwest|hyper|tokio::net|clap::|serve::|calibration::' \
                src/input/model_recipe/profile.rs || true
            )"
            test -z "$unexpected_prepared_profile_authority"
            unexpected_model_publication_authority="$(
              grep -nE \
                'std::fs::(create_dir|hard_link|remove_dir|remove_file|rename|copy|write|set_permissions)|File::create|OpenOptions|hf_hub|std::process::Command|Command::new|reqwest|hyper|tokio::net|clap::|serve::|calibration::' \
                src/input/hf_download/resolution/payload/source/conversion/publication.rs \
                src/input/hf_download/resolution/payload/source/conversion/publication/authentication.rs || true
            )"
            test -z "$unexpected_model_publication_authority"
            unexpected_model_record_file_authority="$(
              grep -nE \
                'std::fs::(create_dir|remove_dir|remove_file|rename|copy|write|set_permissions|hard_link)|File::create|OpenOptions|hf_hub|std::process::Command|Command::new|reqwest|hyper|tokio::net|clap::|serve::|calibration::' \
                src/input/hf_download/resolution/payload/source/conversion/publication/file.rs \
                src/input/hf_download/resolution/payload/source/conversion/publication/file/unix.rs || true
            )"
            test -z "$unexpected_model_record_file_authority"
            model_record_files=(
              src/input/hf_download/resolution/payload/source/conversion/publication/file.rs
              src/input/hf_download/resolution/payload/source/conversion/publication/file/unix.rs
            )
            test "$(grep -Fhc 'fs::linkat(' "${model_record_files[@]}" | awk '{ total += $1 } END { print total }')" = 1
            test "$(grep -Fhc 'fs::unlinkat(' "${model_record_files[@]}" | awk '{ total += $1 } END { print total }')" = 1
            test "$(grep -Fhc 'fs::fchmod(' "${model_record_files[@]}" | awk '{ total += $1 } END { print total }')" = 1
            test "$(grep -Fhc 'OFlags::CREATE' "${model_record_files[@]}" | awk '{ total += $1 } END { print total }')" = 1
            unexpected_setup_authority="$(
              grep -nE \
                'hf_hub|reqwest|hyper|tokio::net|std::net|std::process|Command::new|crate::(models|convert|quantize|serve)|calibration|bootstrap_installation_identity|update_auth|update_transport|remove_dir|remove_file' \
                src/setup/mod.rs src/setup/schema.rs src/setup/host.rs src/setup/policy.rs \
                | grep -vE '^src/setup/schema.rs:[0-9]+:[[:space:]]*crate::convert::QuantSelector::from_name\(&self\.quant\)$' || true
            )"
            test -z "$unexpected_setup_authority"
            test "$(grep -Fhc 'QuantSelector::from_name' src/setup/schema.rs)" = 1
            unexpected_setup_file_authority="$(
              grep -nE \
                'std::fs::(write|copy|create_dir|remove_dir|remove_file|rename|hard_link|set_permissions)|File::create|OpenOptions|hf_hub|reqwest|hyper|tokio::net|std::net|std::process|Command::new|crate::(models|convert|quantize|serve)|calibration|bootstrap_installation_identity|update_auth|update_transport' \
                src/setup/fs.rs src/setup/fs/unix.rs || true
            )"
            test -z "$unexpected_setup_file_authority"
            setup_file_modules=(src/setup/fs.rs src/setup/fs/unix.rs)
            test "$(grep -Fhc 'fs::renameat(' "${setup_file_modules[@]}" | awk '{ total += $1 } END { print total }')" = 1
            test "$(grep -Fhc 'fs::unlinkat(' "${setup_file_modules[@]}" | awk '{ total += $1 } END { print total }')" = 2
            test "$(grep -Fhc 'fs::mkdirat(' "${setup_file_modules[@]}" | awk '{ total += $1 } END { print total }')" = 1
            test "$(grep -Fhc 'OFlags::CREATE' "${setup_file_modules[@]}" | awk '{ total += $1 } END { print total }')" = 1
            test "$(grep -Fhc 'fs::fchmod(' "${setup_file_modules[@]}" | awk '{ total += $1 } END { print total }')" = 1
            test "$(grep -Fhc 'fs::chmodat(' "${setup_file_modules[@]}" | awk '{ total += $1 } END { print total }')" = 1
            test "$(grep -Fhc 'OFlags::EXCL' "${setup_file_modules[@]}" | awk '{ total += $1 } END { print total }')" = 1
            test "$(grep -Fhc 'OFlags::NOFOLLOW' "${setup_file_modules[@]}" | awk '{ total += $1 } END { print total }')" = 3
            test "$(grep -Fhc 'OFlags::NONBLOCK' "${setup_file_modules[@]}" | awk '{ total += $1 } END { print total }')" = 1
            test "$(grep -Fhc 'fs::flock(' "${setup_file_modules[@]}" | awk '{ total += $1 } END { print total }')" = 1
            test "$(grep -Fhc 'fs::fsync(' "${setup_file_modules[@]}" | awk '{ total += $1 } END { print total }')" = 2
            test "$(grep -Fhc 'fs::fcntl_fullfsync(' "${setup_file_modules[@]}" | awk '{ total += $1 } END { print total }')" = 1
            setup_golden=src/setup/testdata/config_v2.toml
            test "$(shasum -a 256 "$setup_golden" | awk '{print $1}')" = \
              ff9f5d3ba200f2ccc107fc6142d9752c87a8429697a6a19f5c67702b82731c17
            recipe=data/model-recipes/qwen38-27b-official-v1.json
            test "$(shasum -a 256 "$recipe" | awk '{print $1}')" = \
              e3b60772d2abf33f14e215ba949bca22d16c0f1d8f985cd172b83b422e205126
            preparation_v1_golden=data/model-recipes/qwen38-27b-preparation-receipt-v1.json
            test "$(shasum -a 256 "$preparation_v1_golden" | awk '{print $1}')" = \
              1f6d98b7269c1754d22248136c2793081dd33ed002a13935bde680fa694dffef
            preparation_golden=data/model-recipes/qwen38-27b-preparation-receipt-v2.json
            test "$(shasum -a 256 "$preparation_golden" | awk '{print $1}')" = \
              b947550f429b7035a97b75a01dc430ba13ff0e83cfbb561d1dab89e926649f36
            prepared_profile_golden=data/model-recipes/qwen38-27b-prepared-profile-v1.json
            test "$(shasum -a 256 "$prepared_profile_golden" | awk '{print $1}')" = \
              bc34868c014b2cd7de650bb2185c79d8a4389f71f68d7611ca6dd6de6c6dd9b0
            fixture=src/distribution/update_auth/testdata/python-tuf-v1
            (cd "$fixture" && shasum -a 256 -c SHA256SUMS)
            expected_generator="$(jq -er .generator_sha256 "$fixture/PROVENANCE.json")"
            actual_generator="$(shasum -a 256 "$fixture/generate.py" | awk '{print $1}')"
            test "$actual_generator" = "$expected_generator"
            for required in \
              src/distribution/schema/first_standalone_record.rs \
              src/distribution/schema/install_receipt.rs \
              src/distribution/schema/install_receipt_validation.rs \
              src/distribution/schema/install_receipt_tests.rs \
              src/distribution/schema/installation_identity.rs \
              src/distribution/schema/installation_identity_tests.rs \
              src/distribution/schema/testdata/installation_identity_v1.json \
              src/distribution/schema/installed_version_marker.rs \
              src/distribution/schema/release_manifest.rs \
              src/distribution/schema/release_manifest_tests.rs \
              src/distribution/schema/testdata/install_receipt_v1_initial_marker_v2.json \
              src/distribution/schema/testdata/installed_version_marker_v1.json \
              src/distribution/schema/testdata/installed_version_marker_v2.json \
              src/distribution/install_state/artifact.rs \
              src/distribution/install_state/artifact/tests.rs \
              src/distribution/install_state/extraction.rs \
              src/distribution/install_state/extraction/io.rs \
              src/distribution/install_state/extraction/modes.rs \
              src/distribution/install_state/extraction/prepared.rs \
              src/distribution/install_state/extraction/prepared/fault.rs \
              src/distribution/install_state/extraction/prepared/support.rs \
              src/distribution/install_state/extraction/prepared/types.rs \
              src/distribution/install_state/extraction_tests.rs \
              src/distribution/install_state/identity.rs \
              src/distribution/install_state/identity/fault.rs \
              src/distribution/install_state/identity/inventory.rs \
              src/distribution/install_state/identity_tests.rs \
              src/distribution/install_state/identity_tests/process_cases.rs \
              src/distribution/install_state/release_floor.rs \
              src/distribution/install_state/release_floor_tests.rs \
              src/distribution/install_state/unix.rs \
              src/distribution/prepared_release/mod.rs \
              src/distribution/prepared_release/archive.rs \
              src/distribution/prepared_release/codesign.rs \
              src/distribution/prepared_release/codesign_tests.rs \
              src/distribution/prepared_release/deflate.rs \
              src/distribution/prepared_release/extract.rs \
              src/distribution/prepared_release/publish.rs \
              src/distribution/prepared_release/macho.rs \
              src/distribution/prepared_release/macho/linkedit.rs \
              src/distribution/prepared_release/macho/segment.rs \
              src/distribution/prepared_release/macho_tests.rs \
              src/distribution/prepared_release/macho_tests/fixture.rs \
              src/distribution/prepared_release/macho_tests/hostile_cases.rs \
              src/distribution/prepared_release/tests.rs \
              src/distribution/update_auth/artifact_authorization.rs \
              src/distribution/update_auth/artifact_authorization/extraction.rs \
              src/distribution/update_auth/commit.rs \
              src/distribution/update_auth/profile.rs \
              src/distribution/update_auth/session.rs \
              src/distribution/update_auth/target_set_tests.rs \
              src/distribution/update_auth/target_set_tests/extraction_cases.rs \
              src/distribution/update_auth/target_set_tests/extraction_cases/prepared_publication_cases.rs \
              src/distribution/update_auth/target_set_tests/extraction_cases/prepared_publication_cases/fault_cases.rs \
              src/distribution/update_auth/target_set_tests/extraction_cases/prepared_publication_cases/hostile_cases.rs \
              src/distribution/update_auth/test_repository.rs \
              src/distribution/update_auth/test_repository/hostile_keys.rs \
              src/distribution/update_auth/tests/hostile_profile.rs \
              src/distribution/update_auth/tests/rotation.rs \
              src/distribution/update_transport/mod.rs \
              src/distribution/update_transport/fetch.rs \
              src/distribution/update_transport/http.rs \
              src/distribution/update_transport/metadata.rs \
              src/distribution/update_transport/origin.rs \
              src/distribution/update_transport/tests.rs \
              src/distribution/update_transport/tests/metadata.rs \
              src/distribution/update_auth/testdata/python-tuf-v1/1.root.json \
              src/distribution/update_auth/testdata/python-tuf-v1/2.root.json \
              src/distribution/update_auth/testdata/python-tuf-v1/timestamp.json \
              src/distribution/update_auth/testdata/python-tuf-v1/2.snapshot.json \
              src/distribution/update_auth/testdata/python-tuf-v1/2.targets.json \
              src/distribution/update_auth/testdata/python-tuf-v1/PROVENANCE.json \
              src/distribution/update_auth/testdata/python-tuf-v1/SHA256SUMS \
              src/distribution/update_auth/testdata/python-tuf-v1/generate.py \
              src/distribution/update_auth/testdata/python-tuf-v1/requirements.lock \
              src/input/hf_reference.rs \
              src/input/hf_reference_tests.rs \
              src/input/hf_download.rs \
              src/input/hf_download/resolution.rs \
              src/input/hf_download/resolution/payload.rs \
              src/input/hf_download/resolution/payload/tests.rs \
              src/input/hf_download/resolution/payload/source.rs \
              src/input/hf_download/resolution/payload/source/tests.rs \
              src/input/hf_download/resolution/payload/source/conversion.rs \
              src/input/hf_download/resolution/payload/source/conversion/tests.rs \
              src/input/hf_download/resolution/payload/source/conversion/publication.rs \
              src/input/hf_download/resolution/payload/source/conversion/publication/authentication.rs \
              src/input/hf_download/resolution/payload/source/conversion/publication/file.rs \
              src/input/hf_download/resolution/payload/source/conversion/publication/file/unix.rs \
              src/input/hf_download/resolution/payload/source/conversion/publication/tests.rs \
              src/input/hf_download/resolution/payload/source/conversion/publication/file/tests.rs \
              src/input/hf_download_plan_tests.rs \
              src/input/integrity.rs \
              src/input/model_recipe.rs \
              src/input/model_recipe/validation.rs \
              src/input/model_recipe/verification.rs \
              src/input/model_recipe/preparation.rs \
              src/input/model_recipe/preparation/host.rs \
              src/input/model_recipe/preparation/validation.rs \
              src/input/model_recipe/plan.rs \
              src/input/model_recipe/profile.rs \
              src/input/model_recipe_tests.rs \
              src/input/model_recipe_preparation_tests.rs \
              src/input/model_recipe_plan_tests.rs \
              src/input/model_recipe_profile_tests.rs \
              data/model-recipes/qwen38-27b-official-v1.json \
              data/model-recipes/qwen38-27b-preparation-receipt-v1.json \
              data/model-recipes/qwen38-27b-preparation-receipt-v2.json \
              data/model-recipes/qwen38-27b-prepared-profile-v1.json \
              src/setup/mod.rs \
              src/setup/schema.rs \
              src/setup/host.rs \
              src/setup/policy.rs \
              src/setup/fs.rs \
              src/setup/fs/unix.rs \
              src/setup/tests.rs \
              src/setup/defaults_contract_tests.rs \
              src/setup/testdata/config_v2.toml \
              src/distribution/mod.rs \
              src/distribution/standalone.rs \
              src/distribution/standalone/update.rs \
              src/distribution/standalone_tests.rs \
              src/distribution/install_state/identity.rs \
              scripts/install.sh.in \
              scripts/render_standalone_installer.sh \
              scripts/render_standalone_release_record.sh \
              scripts/sign_notarize_standalone_release.sh \
              scripts/test_standalone_installer.sh \
              scripts/test_standalone_release_signing_contract.sh \
              Cargo.toml \
              Cargo.lock \
              src/main.rs \
              src/cli.rs \
              src/serve/mod.rs \
              src/core/integrity.rs \
              src/core/provenance/source_shard.rs \
              src/convert/cli_driver.rs \
              src/convert/receipt.rs \
              README.md \
              CHANGELOG.md \
              docs/ADR-045-frictionless-distribution-updates-and-onboarding.md \
              docs/ADR-033-unified-quant-convert-pipeline.md \
              docs/ARCHITECTURE.md \
              docs/setup.md \
              docs/converting-a-model.md \
              docs/getting-started.md
            do
              test -f "$required"
            done
            hf_source_tests="$(cargo test --locked --bin hf2q --all-features -- --list)"
            for required_test in \
              cli::tests::getting_started_qwen38_commands_parse_exactly \
              distribution::standalone::tests::standalone_lifecycle_installs_updates_rolls_back_and_preserves_operator_state \
              distribution::standalone::tests::standalone_installer_bootstrap_surface_is_hidden_bounded_and_parseable \
              distribution::standalone::update::tests::stable_release_record_is_small_canonical_and_exact \
              distribution::standalone::update::tests::standalone_candidate_architecture_is_exactly_thin_arm64 \
              setup::defaults_contract_tests::operator_config_v2_is_canonical_strict_and_uses_the_guide_defaults \
              setup::defaults_contract_tests::operator_config_v2_rejects_incoherent_fifo_slots_and_invalid_quant \
              setup::defaults_contract_tests::absent_operator_config_preserves_existing_command_behavior_without_claiming_root_authority \
              setup::defaults_contract_tests::selected_state_root_loads_the_exact_operator_config \
              setup::tests::cli_parses_the_closed_noninteractive_surface \
              setup::tests::fresh_and_repeated_noninteractive_setup_are_idempotent \
              setup::tests::interactive_setup_uses_current_values_and_records_explicit_operator_choices \
              setup::tests::cancelled_or_interrupted_interactive_setup_creates_nothing \
              setup::tests::noninteractive_setup_requires_complete_choices_or_accept_defaults \
              tests::convert_config_quant_is_used_and_explicit_quant_wins \
              tests::invalid_operator_config_fails_before_convert_source_or_serve_bind \
              serve::tests::serve_config_defaults_are_used_and_cli_env_override_them \
              serve::tests::configured_lan_requires_runtime_auth \
              serve::tests::c4_scheduler_cli_wins_over_env \
              serve::tests::c4_max_slots_cli_wins_over_env \
              setup::tests::matching_existing_installation_identity_is_read_only_and_accepted \
              setup::tests::lock_contention_is_busy_and_mints_no_config \
              setup::tests::exact_read_rejects_leaf_replacement_after_reading_the_open_inode \
              setup::tests::partial_leaf_replacement_before_rename_is_rejected_without_publication \
              setup::tests::config_lock_and_partial_leaf_replacements_never_return_success \
              setup::tests::stale_prompt_snapshot_and_partial_prefix_replacement_fail_closed \
              setup::tests::installation_identity_appearance_or_replacement_during_setup_is_rejected \
              setup::tests::sigabrt_at_every_publication_barrier_recovers_in_a_fresh_process \
              input::hf_reference_tests::equivalent_model_references_share_one_canonical_identity \
              input::hf_download_plan_tests::file_specific_resolution_fails_before_any_hub_lookup \
              input::hf_download_plan_tests::repository_info_seals_a_mutable_request_to_the_returned_exact_commit \
              input::hf_download_plan_tests::immutable_file_metadata_is_checked_before_transfer \
              input::hf_download_plan_tests::metadata_transfer_caps_are_explicit_and_exact \
              input::model_recipe_tests::embedded_recipe_is_exact_canonical_and_binds_accepted_outputs \
              input::model_recipe_tests::semantic_cross_bindings_fail_closed \
              input::model_recipe_preparation_tests::exact_pair_receipt_is_canonical_and_structurally_reparseable \
              input::model_recipe_preparation_tests::volatile_available_space_does_not_change_the_durable_v2_receipt \
              input::model_recipe_preparation_tests::prepublication_v1_receipt_is_retained_as_exact_unsupported_evidence \
              input::model_recipe_preparation_tests::every_conversion_cross_binding_fails_closed \
              input::model_recipe_preparation_tests::os_bound_disk_probe_uses_the_target_filesystem_and_rejects_file_ancestors \
              input::model_recipe_preparation_tests::os_bound_host_preflight_rejects_a_file_as_the_preparation_root \
              input::model_recipe_preparation_tests::current_recipe_host_preflight_passes_on_the_explicit_proof_machine \
              input::model_recipe_plan_tests::canonical_no_options_layout_is_exact_and_inert \
              input::model_recipe_plan_tests::exact_hub_resolution_consumes_the_plan_and_preserves_its_layout \
              input::model_recipe_plan_tests::exact_recipe_metadata_mints_only_an_inert_transfer_authorization \
              input::model_recipe_plan_tests::resolution_must_match_the_original_plan_revision_and_complete_recipe_inventory \
              input::model_recipe_plan_tests::transfer_authorization_rejects_every_metadata_cross_binding \
              input::model_recipe_plan_tests::current_plan_resolves_through_the_exact_production_hub_boundary \
              input::model_recipe_plan_tests::current_plan_authorizes_all_recipe_metadata_before_payload_transfer \
              input::model_recipe_plan_tests::current_no_options_plan_passes_on_the_explicit_proof_machine \
              input::model_recipe_plan_tests::preparation_root_is_absolute_canonical_bounded_and_directory_backed \
              input::model_recipe_profile_tests::prepared_profile_golden_is_exact_and_cross_binds_the_pair_receipt \
              input::model_recipe_profile_tests::parser_rejects_size_duplicate_unknown_trailing_and_noncanonical_bytes \
              input::hf_download::resolution::payload::tests::payload_transfer_consumes_the_exact_recipe_order_and_stays_inert \
              input::hf_download::resolution::payload::tests::failed_or_cross_cache_transfer_mints_no_payload \
              input::hf_download::resolution::payload::tests::namespace_change_is_rejected_before_hub_or_payload_authority \
              input::hf_download::resolution::payload::tests::current_recipe_payload_transfers_and_verifies_when_explicitly_requested \
              input::hf_download::resolution::payload::source::tests::reopened_source_consumes_the_exact_recipe_inventory_and_stays_inert \
              input::hf_download::resolution::payload::source::tests::failed_or_cross_bound_reauthentication_mints_no_source_capability \
              input::hf_download::resolution::payload::source::tests::changed_snapshot_namespace_fails_before_any_source_authentication \
              input::hf_download::resolution::payload::source::tests::snapshot_replacement_during_authentication_mints_no_source_capability \
              input::hf_download::resolution::payload::source::tests::current_recipe_cache_reauthenticates_when_explicitly_requested \
              input::hf_download::resolution::payload::source::conversion::tests::coordinator_reauthenticates_around_both_roles_and_returns_only_inert_pair \
              input::hf_download::resolution::payload::source::conversion::tests::source_drift_after_text_conversion_prevents_projector_and_pair_authority \
              input::hf_download::resolution::payload::source::conversion::tests::projector_failure_prevents_final_reauthentication_and_pair_authority \
              input::hf_download::resolution::payload::source::conversion::tests::final_artifact_drift_prevents_pair_authority_after_both_conversions \
              input::hf_download::resolution::payload::source::conversion::tests::restart_shape_adopts_only_complete_pairs_and_rejects_orphan_receipts \
              input::hf_download::resolution::payload::source::conversion::tests::dangling_symlink_is_retained_as_hostile_evidence_instead_of_absence \
              input::hf_download::resolution::payload::source::conversion::tests::current_recipe_converts_and_reopens_the_exact_pair_when_explicitly_requested \
              input::hf_download::resolution::payload::source::conversion::publication::tests::publication_reauthenticates_before_and_after_each_durable_record \
              input::hf_download::resolution::payload::source::conversion::publication::tests::restart_order_accepts_only_write_order_prefixes_and_exact_hardlink_residue \
              input::hf_download::resolution::payload::source::conversion::publication::file::tests::exact_prefix_and_published_hardlink_crash_states_are_resumed \
              input::hf_download::resolution::payload::source::conversion::publication::file::tests::hostile_partial_final_and_symlink_evidence_is_retained \
              input::hf_download::resolution::payload::source::conversion::publication::file::tests::every_publication_barrier_is_exactly_retryable \
              input::hf_download::resolution::payload::source::conversion::publication::file::tests::concurrent_publishers_cannot_append_after_a_measured_partial_prefix \
              input::hf_download::resolution::payload::source::conversion::publication::file::tests::exact_read_rejects_leaf_replacement_after_the_open_fd_was_read \
              input::hf_download::resolution::payload::source::conversion::publication::file::tests::retained_parent_descriptor_prevents_namespace_redirection \
              input::hf_download::resolution::payload::source::conversion::publication::file::tests::restrictive_umask_cannot_strand_an_unusable_partial \
              input::model_recipe_plan_tests::completed_conversion_files_must_be_canonical_single_link_regular_files \
              convert::cli_driver::tests::deepseek_tiny_official_layout_converts_to_q2_k_s_end_to_end \
              convert::cli_driver::tests::explicit_receipt_path_cannot_alias_the_conversion_output \
              models::vit::gguf_emit::tests::projector_can_reproduce_a_recipe_frozen_producer_banner \
              input::integrity::tests::safetensors_index_entry_cap_is_exact \
              convert::receipt::tests::prepared_success_receipt_binds_output_and_replaces_stale_atomically \
              convert::receipt::tests::explicit_receipt_destination_cannot_replace_the_artifact \
              tests::convert_source_classifier_reconciles_revision_and_rejects_ambiguity
            do
              grep -Fq -- "$required_test: test" <<<"$hf_source_tests"
            done
            cargo test --locked --bin hf2q --all-features input::hf_reference_tests::
            cargo test --locked --bin hf2q --all-features input::hf_download_plan_tests::
            cargo test --locked --bin hf2q --all-features input::model_recipe_tests::
            cargo test --locked --bin hf2q --all-features input::model_recipe_preparation_tests::
            cargo test --locked --bin hf2q --all-features input::model_recipe_plan_tests::
            cargo test --locked --bin hf2q --all-features input::model_recipe_profile_tests::
            cargo test --locked --bin hf2q --all-features input::hf_download::resolution::payload::tests:: -- --test-threads=1
            cargo test --locked --bin hf2q --all-features input::hf_download::resolution::payload::source::tests:: -- --test-threads=1
            cargo test --locked --bin hf2q --all-features input::hf_download::resolution::payload::source::conversion::tests:: -- --test-threads=1
            cargo test --locked --bin hf2q --all-features input::hf_download::resolution::payload::source::conversion::publication:: -- --test-threads=1
            cargo test --locked --bin hf2q --all-features input::integrity::tests:: -- --test-threads=1
            cargo test --locked --bin hf2q --all-features convert::cli_driver::tests::deepseek_tiny_official_layout_converts_to_q2_k_s_end_to_end -- --exact
            cargo test --locked --bin hf2q --all-features convert::cli_driver::tests::explicit_receipt_path_cannot_alias_the_conversion_output -- --exact
            cargo test --locked --bin hf2q --all-features convert::receipt::tests::
            cargo test --locked --bin hf2q --all-features models::vit::gguf_emit::tests::projector_can_reproduce_a_recipe_frozen_producer_banner -- --exact
            cargo test --locked --bin hf2q --all-features tests::convert_source_classifier
            distribution_tests="$(cargo test --locked --bin hf2q --all-features -- --list)"
            for required_test in \
              distribution::install_state::identity::tests::process_cases::process_abort_at_every_identity_barrier_is_exactly_recoverable \
              distribution::install_state::identity::tests::process_cases::shared_identity_lock_excludes_a_fresh_process_bootstrap \
              distribution::install_state::extraction::tests::process_abort_mid_file_leaves_only_resumable_inert_state \
              distribution::install_state::extraction::tests::every_normalization_barrier_is_exactly_restartable_after_sigabrt \
              distribution::install_state::extraction::tests::every_normalization_barrier_is_exactly_retryable_after_returned_error \
              distribution::update_auth::target_set::tests::extraction_cases::stored_and_deflated_bundles_cross_the_complete_extraction_boundary \
              distribution::update_auth::target_set::tests::extraction_cases::signed_mode_normalization_is_sealed_and_keeps_the_tree_inert \
              distribution::update_auth::target_set::tests::extraction_cases::failed_native_brackets_return_no_capability_and_exact_retry_recovers \
              distribution::update_auth::target_set::tests::extraction_cases::post_extraction_archive_revalidation_failure_returns_no_capability_and_is_retryable
            do
              grep -Fq -- "$required_test: test" <<<"$distribution_tests"
            done
            cargo test --locked --bin hf2q --all-features qwen38 -- --test-threads=1
            cargo test --locked --bin hf2q --all-features qwen35_serving_tokenizer_is_sidecar_free_and_detects_vision_markers -- --test-threads=1
            cargo test --locked --bin hf2q --all-features vision_projector_binding -- --test-threads=1
            cargo test --locked --bin hf2q --all-features model_advertisement_contract_ -- --test-threads=1
            cargo test --locked --bin hf2q --all-features qwen35_startup_warmup_primes_gpu_for_serial_and_slot_aware -- --test-threads=1
            cargo test --locked --bin hf2q --all-features qwen35_bounded_prefill_watchdog_tests -- --test-threads=1
            cargo test --locked --bin hf2q --all-features prompt_cache_ -- --test-threads=1
            cargo test --locked --bin hf2q --all-features agentic_grammar_contract_ -- --test-threads=1
            cargo test --locked --bin hf2q --all-features gemma4_bounded_prefill_tests -- --test-threads=1
            cargo test --locked --bin hf2q --all-features slotaware_fail_stop_tests -- --test-threads=1
            cargo test --locked --bin hf2q --all-features engine_supervisor::tests -- --test-threads=1
            cargo test --locked --bin hf2q --all-features public_watchdog_fixture_bytes_are_stable_without_a_model -- --test-threads=1
            cargo test --locked --bin hf2q --all-features readiness_guard_tests -- --test-threads=1
            bash -n scripts/qwen36_watchdog_validate.sh \
              scripts/serve_qwen38_opencode.sh \
              scripts/test_qwen36_prefill_watchdog.sh \
              scripts/test_qwen36_prefill_cancellation.sh \
              scripts/test_qwen36_cumulative_release.sh \
              scripts/test_deepseek4_agentic.sh \
              scripts/test_deepseek4_structured_tools.sh \
              scripts/test_deepseek4_agentic_fixture_contract.sh \
              scripts/test_deepseek4_decode_cohort_receipt_contract.sh \
              scripts/test_deepseek4_peer_cold_wave.sh \
              scripts/test_deepseek4_peer_cold_wave_contract.sh \
              scripts/run_deepseek4_matched_peer.sh \
              scripts/macos_thermal_guard.sh \
              scripts/verify_macos_thermal_receipt.sh \
              scripts/verify_deepseek4_decode_cohort_receipt.sh \
              scripts/run_deepseek4_decode_cohort_gate.sh \
              scripts/verify_gemma4_wave_thermal_receipt.sh \
              scripts/verify_gemma4_parity_receipt.sh \
              scripts/qwen38_long_decode_ab.sh \
              scripts/verify_qwen38_long_decode_receipt.sh \
              scripts/test_qwen38_long_decode_receipt_contract.sh \
              scripts/test_macos_thermal_guard_contract.sh \
              scripts/test_gemma4_wave_thermal_contract.sh \
              scripts/test_gemma4_eight_slot_receipt_contract.sh \
              scripts/verify_release_dependency_provenance.sh \
              scripts/test_release_dependency_provenance_contract.sh \
              scripts/seal_release_binary.sh \
              scripts/test_release_binary_seal_contract.sh \
              scripts/sign_notarize_standalone_release.sh \
              scripts/test_standalone_release_signing_contract.sh \
              scripts/run_release_gate_process_group.sh \
              scripts/test_release_gate_process_group_contract.sh \
              scripts/ensure_github_release_tag.sh \
              scripts/test_release_tag_recovery_contract.sh \
              scripts/test_deepseek4_cached_suffix.sh \
              scripts/test_deepseek4_cached_suffix_contract.sh \
              scripts/test_deepseek4_interactive_overlap.sh \
              scripts/test_agentic_cache_lifecycle.sh \
              scripts/test_gemma4_long_short_overlap.sh \
              scripts/test_gemma4_long_short_overlap_contract.sh \
              scripts/run_agentic_cache_release_gate.sh
            bash scripts/test_qwen36_watchdog_harness_contract.sh
            bash scripts/test_deepseek4_cached_suffix_contract.sh
            bash scripts/test_deepseek4_agentic_fixture_contract.sh
            bash scripts/test_deepseek4_decode_cohort_receipt_contract.sh
            bash scripts/test_deepseek4_peer_cold_wave_contract.sh
            bash scripts/test_macos_thermal_guard_contract.sh
            bash scripts/test_gemma4_wave_thermal_contract.sh
            bash scripts/test_gemma4_eight_slot_receipt_contract.sh
            bash scripts/test_qwen38_long_decode_receipt_contract.sh
            bash scripts/test_release_dependency_provenance_contract.sh
            bash scripts/test_release_binary_seal_contract.sh
            bash scripts/test_standalone_release_signing_contract.sh
            bash scripts/test_release_gate_process_group_contract.sh
            bash scripts/test_release_tag_recovery_contract.sh
            bash scripts/test_gemma4_long_short_overlap_contract.sh
            cargo test --locked --bin hf2q --all-features deepseek4 -- \
              --skip attention_forward_tests \
              --skip allocator_materializes_the_plan_as_zeroed_bf16_buffers \
              --skip cache_steps_publish_only_complete_groups_and_commit_transactionally \
              --skip partial_token_poison_requires_reset_before_replay \
              --skip start_zero_prefill_span_counts_complete_groups_and_publishes_once \
              --skip ffn_forward_tests \
              --skip raw_matmul_accepts_quality_sensitive_f32_weights \
              --skip embedding_forward_rejects_empty_input \
              --skip q2_k_embeddings_expand_to_four_identical_hc_streams \
              --skip native_model_load_keeps_weights_and_cache_on_one_device \
              --skip native_output_head_produces_finite_vocab_logits_and_rejects_shape_drift \
              --skip loader_preserves_raw_blocks_and_expands_only_elementwise_state \
              --skip loader_rejects_catalog_and_i32_storage_before_residency
          )
          install_root="$RUNNER_TEMP/hf2q-install"
          cargo install \
            --path "$package_root" \
            --locked \
            --root "$install_root"
          "$install_root/bin/hf2q" --help >/dev/null

      - name: Prepare and locally prove standalone release assets
        shell: bash
        run: |
          set -euo pipefail
          standalone_stage=$(mktemp -d "$RUNNER_TEMP/hf2q-standalone-release.XXXXXX")
          source_dir=${STANDALONE_RELEASE_DIR:?STANDALONE_RELEASE_DIR is required}
          binary="$standalone_stage/hf2q-aarch64-apple-darwin"
          cp "$source_dir/hf2q-aarch64-apple-darwin" "$binary"
          chmod 0555 "$binary"
          cp "$source_dir/hf2q-aarch64-apple-darwin.sha256" \
            "$standalone_stage/hf2q-aarch64-apple-darwin.sha256"
          cp "$source_dir/proof.json" \
            "$standalone_stage/hf2q-aarch64-apple-darwin.proof.json"
          cp "$source_dir/notary-log.json" \
            "$standalone_stage/hf2q-aarch64-apple-darwin.notary-log.json"
          test "$(shasum -a 256 "$binary" | awk '{print $1}')" = \
            "$STANDALONE_BINARY_SHA256"
          test "$(stat -f '%z' "$binary")" = "$STANDALONE_BINARY_SIZE"
          test "$(/usr/bin/lipo -archs "$binary")" = arm64
          test "$(/usr/bin/vtool -show-build "$binary" | awk '$1 == "minos" {print $2}')" = 14.0
          /usr/bin/codesign --verify --strict --all-architectures "$binary"
          /usr/sbin/spctl --assess --type execute "$binary"
          test "$(HF2Q_NO_COMPLETION_INSTALL=1 "$binary" --version)" = \
            "hf2q $EXPECTED_VERSION"

          scripts/render_standalone_release_record.sh \
            "$standalone_stage/stable-aarch64-apple-darwin.json" \
            "$EXPECTED_VERSION" "$STANDALONE_BINARY_SIZE" \
            "$STANDALONE_BINARY_SHA256"
          scripts/render_standalone_installer.sh \
            scripts/install.sh.in "$standalone_stage/install.sh" \
            "$EXPECTED_VERSION" "$STANDALONE_BINARY_SIZE" \
            "$STANDALONE_BINARY_SHA256" "$STANDALONE_TEAM_ID" \
            "$STANDALONE_IDENTIFIER"

          proof_home=$(mktemp -d "$RUNNER_TEMP/hf2q-standalone-install-home.XXXXXX")
          proof_install="$proof_home/.local/bin"
          proof_state="$proof_home/.hf2q"
          mkdir -p "$proof_state/models"
          printf 'operator config\n' > "$proof_state/config.toml"
          printf 'converted model\n' > "$proof_state/models/model.gguf"
          curl -fsSL "file://$standalone_stage/install.sh" | \
            HOME="$proof_home" \
            HF2Q_INSTALL_DIR="$proof_install" \
            HF2Q_RELEASE_BASE_URL="file://$standalone_stage" \
            PATH=/usr/bin:/bin:/usr/sbin:/sbin \
            sh >/dev/null
          test "$(shasum -a 256 "$proof_install/hf2q" | awk '{print $1}')" = \
            "$STANDALONE_BINARY_SHA256"
          test "$(HF2Q_NO_COMPLETION_INSTALL=1 "$proof_install/hf2q" --version)" = \
            "hf2q $EXPECTED_VERSION"
          "$proof_install/hf2q" uninstall --yes >/dev/null
          test ! -e "$proof_install/hf2q"
          test -f "$proof_state/config.toml"
          test -f "$proof_state/models/model.gguf"
          echo "STANDALONE_STAGE=$standalone_stage" >> "$GITHUB_ENV"

      - name: Create draft release and attach exact assets once
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          tag="v${EXPECTED_VERSION}"
          bash scripts/ensure_github_release_tag.sh \
            "$GITHUB_REPOSITORY" "$tag" "$EXPECTED_SHA"
          if ! gh release view "$tag" >/dev/null 2>&1; then
            gh release create "$tag" \
              --target "$EXPECTED_SHA" \
              --title "hf2q ${EXPECTED_VERSION}" \
              --generate-notes \
              --draft \
              --verify-tag
          fi
          release_json=$(gh release view "$tag" --json isDraft,targetCommitish)
          test "$(jq -r .targetCommitish <<<"$release_json")" = "$EXPECTED_SHA"
          is_draft=$(jq -r .isDraft <<<"$release_json")

          ensure_exact_asset() {
            local asset=$1
            local name directory
            name=$(basename "$asset")
            directory=$(mktemp -d "$RUNNER_TEMP/hf2q-existing-asset.XXXXXX")
            if gh release download "$tag" --pattern "$name" --dir "$directory" \
              >/dev/null 2>&1; then
              cmp -s "$asset" "$directory/$name" || {
                echo "published release asset differs from candidate: $name" >&2
                return 1
              }
            else
              test "$is_draft" = true || {
                echo "published release is missing immutable asset: $name" >&2
                return 1
              }
              gh release upload "$tag" "$asset"
            fi
          }

          for asset in \
            "$STANDALONE_STAGE/hf2q-aarch64-apple-darwin" \
            "$STANDALONE_STAGE/hf2q-aarch64-apple-darwin.sha256" \
            "$STANDALONE_STAGE/hf2q-aarch64-apple-darwin.proof.json" \
            "$STANDALONE_STAGE/hf2q-aarch64-apple-darwin.notary-log.json" \
            "$STANDALONE_STAGE/stable-aarch64-apple-darwin.json" \
            "$STANDALONE_STAGE/install.sh" \
            "target/package/hf2q-${EXPECTED_VERSION}.crate" \
            "$RUNNER_TEMP/release-crate.sha256"; do
            ensure_exact_asset "$asset"
          done

      - name: Verify every draft release asset from a fresh download
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          tag="v${EXPECTED_VERSION}"
          release_dir=$(mktemp -d "$RUNNER_TEMP/hf2q-draft-release.XXXXXX")
          gh release download "$tag" --dir "$release_dir"
          for asset in \
            "$STANDALONE_STAGE/hf2q-aarch64-apple-darwin" \
            "$STANDALONE_STAGE/hf2q-aarch64-apple-darwin.sha256" \
            "$STANDALONE_STAGE/hf2q-aarch64-apple-darwin.proof.json" \
            "$STANDALONE_STAGE/hf2q-aarch64-apple-darwin.notary-log.json" \
            "$STANDALONE_STAGE/stable-aarch64-apple-darwin.json" \
            "$STANDALONE_STAGE/install.sh" \
            "target/package/hf2q-${EXPECTED_VERSION}.crate" \
            "$RUNNER_TEMP/release-crate.sha256"; do
            cmp -s "$asset" "$release_dir/$(basename "$asset")"
          done

      - name: Publish exact package
        shell: bash
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          set -euo pipefail
          test -n "$CARGO_REGISTRY_TOKEN"
          published="$RUNNER_TEMP/hf2q-${EXPECTED_VERSION}-published.crate"
          if curl --fail --location --silent --show-error \
            "https://static.crates.io/crates/hf2q/hf2q-${EXPECTED_VERSION}.crate" \
            --output "$published"; then
            echo "hf2q ${EXPECTED_VERSION} is already published; verifying exact bytes"
          else
            # The exact package was already compiled and tested above. Avoid
            # re-running dependency build scripts while the registry token is
            # present in this narrowly scoped step.
            cargo publish --locked --no-verify --token "$CARGO_REGISTRY_TOKEN"
          fi

      - name: Verify crates.io bytes
        shell: bash
        run: |
          set -euo pipefail
          expected=$(awk '{print $1}' "$RUNNER_TEMP/release-crate.sha256")
          downloaded="$RUNNER_TEMP/hf2q-${EXPECTED_VERSION}.crate"
          for _ in $(seq 1 24); do
            if curl --fail --location --silent --show-error \
              "https://static.crates.io/crates/hf2q/hf2q-${EXPECTED_VERSION}.crate" \
              --output "$downloaded"; then
              actual=$(shasum -a 256 "$downloaded" | awk '{print $1}')
              test "$actual" = "$expected"
              exit 0
            fi
            sleep 5
          done
          exit 1

      - name: Install and smoke-test crates.io artifact
        shell: bash
        run: |
          set -euo pipefail
          downloaded="$RUNNER_TEMP/hf2q-${EXPECTED_VERSION}.crate"
          registry_root="$RUNNER_TEMP/hf2q-registry"
          install_root="$RUNNER_TEMP/hf2q-registry-install"
          mkdir -p "$registry_root"
          tar -xzf "$downloaded" -C "$registry_root"
          cargo install \
            --path "$registry_root/hf2q-${EXPECTED_VERSION}" \
            --locked \
            --root "$install_root"
          "$install_root/bin/hf2q" --help >/dev/null

      - name: Publish the complete GitHub release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          tag="v${EXPECTED_VERSION}"
          release_json=$(gh release view "$tag" --json isDraft,targetCommitish)
          test "$(jq -r .targetCommitish <<<"$release_json")" = "$EXPECTED_SHA"
          if [[ $(jq -r .isDraft <<<"$release_json") == true ]]; then
            gh release edit "$tag" --draft=false
          fi
          release_json=$(gh release view "$tag" --json isDraft,targetCommitish)
          test "$(jq -r .targetCommitish <<<"$release_json")" = "$EXPECTED_SHA"
          test "$(jq -r .isDraft <<<"$release_json")" = false

      - name: Verify public GitHub release bytes and clean-prefix install
        shell: bash
        run: |
          set -euo pipefail
          tag="v${EXPECTED_VERSION}"
          public_base="https://github.com/$GITHUB_REPOSITORY/releases/download/$tag"
          release_dir=$(mktemp -d "$RUNNER_TEMP/hf2q-public-release.XXXXXX")
          for name in \
            hf2q-aarch64-apple-darwin \
            hf2q-aarch64-apple-darwin.sha256 \
            hf2q-aarch64-apple-darwin.proof.json \
            hf2q-aarch64-apple-darwin.notary-log.json \
            stable-aarch64-apple-darwin.json \
            install.sh \
            "hf2q-${EXPECTED_VERSION}.crate" \
            release-crate.sha256; do
            downloaded=0
            for _ in $(seq 1 24); do
              if curl --fail --location --silent --show-error \
                "$public_base/$name" --output "$release_dir/$name"; then
                downloaded=1
                break
              fi
              sleep 5
            done
            test "$downloaded" = 1
          done
          for asset in \
            "$STANDALONE_STAGE/hf2q-aarch64-apple-darwin" \
            "$STANDALONE_STAGE/hf2q-aarch64-apple-darwin.sha256" \
            "$STANDALONE_STAGE/hf2q-aarch64-apple-darwin.proof.json" \
            "$STANDALONE_STAGE/hf2q-aarch64-apple-darwin.notary-log.json" \
            "$STANDALONE_STAGE/stable-aarch64-apple-darwin.json" \
            "$STANDALONE_STAGE/install.sh" \
            "target/package/hf2q-${EXPECTED_VERSION}.crate" \
            "$RUNNER_TEMP/release-crate.sha256"; do
            cmp -s "$asset" "$release_dir/$(basename "$asset")"
          done
          public_binary="$release_dir/hf2q-aarch64-apple-darwin"
          chmod 0555 "$public_binary"
          test "$(shasum -a 256 "$public_binary" | awk '{print $1}')" = \
            "$STANDALONE_BINARY_SHA256"
          test "$(/usr/bin/lipo -archs "$public_binary")" = arm64
          /usr/bin/codesign --verify --strict --all-architectures "$public_binary"
          /usr/sbin/spctl --assess --type execute "$public_binary"
          test "$(HF2Q_NO_COMPLETION_INSTALL=1 "$public_binary" --version)" = \
            "hf2q $EXPECTED_VERSION"

          proof_home=$(mktemp -d "$RUNNER_TEMP/hf2q-public-install-home.XXXXXX")
          proof_install="$proof_home/.local/bin"
          proof_state="$proof_home/.hf2q"
          mkdir -p "$proof_state/models"
          printf 'operator config\n' > "$proof_state/config.toml"
          printf 'converted model\n' > "$proof_state/models/model.gguf"
          curl -fsSL "$public_base/install.sh" | \
            HOME="$proof_home" HF2Q_INSTALL_DIR="$proof_install" \
            PATH=/usr/bin:/bin:/usr/sbin:/sbin sh >/dev/null
          test "$(shasum -a 256 "$proof_install/hf2q" | awk '{print $1}')" = \
            "$STANDALONE_BINARY_SHA256"
          test "$(HF2Q_NO_COMPLETION_INSTALL=1 "$proof_install/hf2q" --version)" = \
            "hf2q $EXPECTED_VERSION"
          "$proof_install/hf2q" uninstall --yes >/dev/null
          test ! -e "$proof_install/hf2q"
          test -f "$proof_state/config.toml"
          test -f "$proof_state/models/model.gguf"
